{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "packages/cli",
  "sourceRoot": "packages/cli/src",
  "projectType": "library",
  "tags": ["scope:typescript"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run --config vitest.config.ts",
        "cwd": "{projectRoot}"
      }
    }
  }
}
