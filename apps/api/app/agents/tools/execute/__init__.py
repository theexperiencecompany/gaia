"""The execute proxy: one tool that resolves, validates and runs integration tools.

Dispatch core shared by the LLM-facing ``execute`` tool and the sandbox
code-mode route — resolution and validation live in exactly one place.
"""
