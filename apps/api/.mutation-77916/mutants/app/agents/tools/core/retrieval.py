"""  # pragma: no mutate
Tool Retrieval Functions for Agent Tool Discovery & Binding.  # pragma: no mutate

This module provides the retrieve_tools function factory that supports:  # pragma: no mutate
- Semantic search for tool discovery (query mode)  # pragma: no mutate
- Exact tool binding (exact_tool_names mode)  # pragma: no mutate
- Namespace filtering for user's connected integrations  # pragma: no mutate
- Subagent filtering based on user's connected integrations  # pragma: no mutate
"""  # pragma: no mutate

import asyncio  # pragma: no mutate
from collections.abc import Awaitable, Callable  # pragma: no mutate
import json
from typing import (  # pragma: no mutate
    Annotated,  # pragma: no mutate
    Any,  # pragma: no mutate
    TypeAlias,  # pragma: no mutate
    TypedDict,  # pragma: no mutate
    Union,  # pragma: no mutate
    cast,  # pragma: no mutate
)  # pragma: no mutate

from langchain_core.runnables import RunnableConfig  # pragma: no mutate
from langgraph.prebuilt import InjectedStore  # pragma: no mutate
from langgraph.store.base import BaseStore, SearchItem  # pragma: no mutate
from pydantic import Field  # pragma: no mutate

from app.agents.core.subagents.registry import all_subagents, get_subagent_by_id  # pragma: no mutate
from app.agents.tools.core.registry import (  # pragma: no mutate
    DESKTOP_TOOL_CATEGORY,  # pragma: no mutate
    DESKTOP_TOOL_SPACE,  # pragma: no mutate
    ToolRegistry,  # pragma: no mutate
    get_tool_registry,  # pragma: no mutate
)  # pragma: no mutate
from app.agents.tools.research_tool import deep_research  # pragma: no mutate
from app.agents.tools.webpage_tool import fetch_webpages, web_search_tool  # pragma: no mutate
from app.config.oauth_config import OAUTH_INTEGRATIONS  # pragma: no mutate
from app.constants.log_tags import LogTag  # pragma: no mutate
from app.db.chroma.public_integrations_store import search_public_integrations  # pragma: no mutate
from app.models.agent_models import agent_configurable  # pragma: no mutate
from app.models.chat_models import ConversationSource  # pragma: no mutate
from app.override.langgraph_bigtool.utils import RetrieveToolsResult
from app.services.integrations.integration_service import (  # pragma: no mutate
    get_user_available_tool_namespaces,  # pragma: no mutate
)  # pragma: no mutate
from app.services.integrations.user_integrations import get_user_integrations  # pragma: no mutate
from app.services.mcp.mcp_client import get_mcp_client  # pragma: no mutate
from app.services.oauth.oauth_service import get_all_integrations_status  # pragma: no mutate
from app.utils.mcp_utils import canonical_tool_name_map  # pragma: no mutate
from shared.py.wide_events import log  # pragma: no mutate

WEBPAGE_TOOLS = [web_search_tool.name, fetch_webpages.name, deep_research.name]  # pragma: no mutate


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__user_mcp_tool_names__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__user_mcp_tool_names__mutmut)
async def _user_mcp_tool_names(user_id: str | None) -> set[str]:  # pragma: no mutate
    """Tool names exposed by the user's live MCPClient.  # pragma: no mutate

    The resilience rewrite moved per-user MCP tool storage out of the global  # pragma: no mutate
    ToolRegistry, so `retrieve_tools` can't rely on `get_tool_names()` alone  # pragma: no mutate
    for discovery filtering or binding validation. This helper supplies the  # pragma: no mutate
    missing slice — read straight from the MCPClient that owns the live  # pragma: no mutate
    connectors for `user_id`. Returns an empty set on any failure so the  # pragma: no mutate
    surrounding logic degrades cleanly.  # pragma: no mutate
    """  # pragma: no mutate
    if not user_id:  # pragma: no mutate
        return set()  # pragma: no mutate
    try:  # pragma: no mutate
        mcp_client = await get_mcp_client(user_id=str(user_id))  # pragma: no mutate
        names: set[str] = set()  # pragma: no mutate
        for integration_tools in mcp_client._tools.values():  # pragma: no mutate
            names.update(t.name for t in integration_tools)  # pragma: no mutate
        return names  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} _user_mcp_tool_names failed",  # pragma: no mutate
            user_id=user_id,  # pragma: no mutate
            error_type=type(e).__name__,  # pragma: no mutate
        )  # pragma: no mutate
        return set()  # pragma: no mutate


async def x__user_mcp_tool_names__mutmut_orig(user_id: str | None) -> set[str]:  # pragma: no mutate
    """Tool names exposed by the user's live MCPClient.  # pragma: no mutate

    The resilience rewrite moved per-user MCP tool storage out of the global  # pragma: no mutate
    ToolRegistry, so `retrieve_tools` can't rely on `get_tool_names()` alone  # pragma: no mutate
    for discovery filtering or binding validation. This helper supplies the  # pragma: no mutate
    missing slice — read straight from the MCPClient that owns the live  # pragma: no mutate
    connectors for `user_id`. Returns an empty set on any failure so the  # pragma: no mutate
    surrounding logic degrades cleanly.  # pragma: no mutate
    """  # pragma: no mutate
    if not user_id:  # pragma: no mutate
        return set()  # pragma: no mutate
    try:  # pragma: no mutate
        mcp_client = await get_mcp_client(user_id=str(user_id))  # pragma: no mutate
        names: set[str] = set()  # pragma: no mutate
        for integration_tools in mcp_client._tools.values():  # pragma: no mutate
            names.update(t.name for t in integration_tools)  # pragma: no mutate
        return names  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} _user_mcp_tool_names failed",  # pragma: no mutate
            user_id=user_id,  # pragma: no mutate
            error_type=type(e).__name__,  # pragma: no mutate
        )  # pragma: no mutate
        return set()  # pragma: no mutate


async def x__user_mcp_tool_names__mutmut_1(user_id: str | None) -> set[str]:  # pragma: no mutate
    """Tool names exposed by the user's live MCPClient.  # pragma: no mutate

    The resilience rewrite moved per-user MCP tool storage out of the global  # pragma: no mutate
    ToolRegistry, so `retrieve_tools` can't rely on `get_tool_names()` alone  # pragma: no mutate
    for discovery filtering or binding validation. This helper supplies the  # pragma: no mutate
    missing slice — read straight from the MCPClient that owns the live  # pragma: no mutate
    connectors for `user_id`. Returns an empty set on any failure so the  # pragma: no mutate
    surrounding logic degrades cleanly.  # pragma: no mutate
    """  # pragma: no mutate
    if not user_id:  # pragma: no mutate
        return set()  # pragma: no mutate
    try:  # pragma: no mutate
        mcp_client = await get_mcp_client(user_id=str(user_id))  # pragma: no mutate
        names: set[str] = set()  # pragma: no mutate
        for integration_tools in mcp_client._tools.values():  # pragma: no mutate
            names.update(t.name for t in integration_tools)  # pragma: no mutate
        return names  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} _user_mcp_tool_names failed",  # pragma: no mutate
            user_id=user_id,  # pragma: no mutate
            error_type=type(None).__name__,  # pragma: no mutate
        )  # pragma: no mutate
        return set()  # pragma: no mutate

mutants_x__user_mcp_tool_names__mutmut['_mutmut_orig'] = x__user_mcp_tool_names__mutmut_orig # type: ignore # mutmut generated
mutants_x__user_mcp_tool_names__mutmut['x__user_mcp_tool_names__mutmut_1'] = x__user_mcp_tool_names__mutmut_1 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__is_platform_tool_space__mutmut)
def _is_platform_tool_space(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True  # pragma: no mutate
        and integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_orig(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True  # pragma: no mutate
        and integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_1(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True  # pragma: no mutate
        and integration.subagent_config is not None or integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_2(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True or integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_3(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is not True  # pragma: no mutate
        and integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_4(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is False  # pragma: no mutate
        and integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_5(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True  # pragma: no mutate
        and integration.subagent_config is None  # pragma: no mutate
        and integration.subagent_config.tool_space == tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate


def x__is_platform_tool_space__mutmut_6(tool_space: str) -> bool:  # pragma: no mutate
    """True if `tool_space` belongs to a hardcoded platform integration.  # pragma: no mutate

    Platform integrations (github, gmail, slack, ...) are defined in  # pragma: no mutate
    OAUTH_INTEGRATIONS and have a fixed `subagent_config.tool_space`.  # pragma: no mutate
    Their tool descriptions are not user-owned, so it is safe to search  # pragma: no mutate
    them without checking that the caller's `user_namespaces` lists them.  # pragma: no mutate

    Custom MCPs and user-added integrations have dynamic, user-owned  # pragma: no mutate
    namespaces (e.g. URL-derived). Those MUST stay gated by user_namespaces  # pragma: no mutate
    so one user cannot search another user's MCP tools.  # pragma: no mutate
    """  # pragma: no mutate
    return any(  # pragma: no mutate
        integration.available is True  # pragma: no mutate
        and integration.subagent_config is not None  # pragma: no mutate
        and integration.subagent_config.tool_space != tool_space  # pragma: no mutate
        for integration in OAUTH_INTEGRATIONS  # pragma: no mutate
    )  # pragma: no mutate

mutants_x__is_platform_tool_space__mutmut['_mutmut_orig'] = x__is_platform_tool_space__mutmut_orig # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_1'] = x__is_platform_tool_space__mutmut_1 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_2'] = x__is_platform_tool_space__mutmut_2 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_3'] = x__is_platform_tool_space__mutmut_3 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_4'] = x__is_platform_tool_space__mutmut_4 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_5'] = x__is_platform_tool_space__mutmut_5 # type: ignore # mutmut generated
mutants_x__is_platform_tool_space__mutmut['x__is_platform_tool_space__mutmut_6'] = x__is_platform_tool_space__mutmut_6 # type: ignore # mutmut generated


# ---------------------------------------------------------------------------
# retrieve_tools docstring (doubles as LLM-facing tool description)
# ---------------------------------------------------------------------------
# The base docstring covers discovery and binding modes. The subagent section
# is appended only when include_subagents=True so that provider/spawned
# subagents never see delegation guidance they can't act on.

_RETRIEVE_TOOLS_BASE_DOC = """\
Discover and load tools for execution. Supports two modes: discovery and binding.  # pragma: no mutate

REQUIRED: pass exactly ONE of `query` (to discover by intent) or `exact_tool_names`  # pragma: no mutate
(to bind known names). Calling with NEITHER argument is invalid and returns corrective  # pragma: no mutate
guidance instead of binding anything. If you are looking for a capability, pass  # pragma: no mutate
query="what you want to do".  # pragma: no mutate

—DISCOVERY MODE (query)  # pragma: no mutate
Semantic search that returns tool names matching your intent. Tools are NOT loaded yet.  # pragma: no mutate

Rules:  # pragma: no mutate
- This is semantic vector search over tool names and descriptions. Phrase the query in  # pragma: no mutate
  natural language using the INTEGRATION'S NAME ("github", "gmail", "notion") and the  # pragma: no mutate
  action — NEVER an id, uuid, slug, or internal key. Ids carry no semantic meaning, so  # pragma: no mutate
  they embed to noise and return irrelevant tools. Search "list github pull requests",  # pragma: no mutate
  not the repo/connection id.  # pragma: no mutate
- One well-formed query is enough for most tasks. Do not retry unless results are clearly irrelevant.  # pragma: no mutate
- Do not search repeatedly to be thorough. If the first result looks right, move to binding.  # pragma: no mutate
- Comma-separated intents work: "list pull requests, get repo info"  # pragma: no mutate

—BINDING MODE (exact_tool_names)  # pragma: no mutate
Loads tools by exact name so they can be called. Use this after discovery or when you already know the name.  # pragma: no mutate

Rules:  # pragma: no mutate
- Only bind tools you are about to use in the next 1-2 steps  # pragma: no mutate
- Unknown or invalid names are silently ignored  # pragma: no mutate
- You CANNOT call a tool that has not been bound first  # pragma: no mutate

—STANDARD WORKFLOW  # pragma: no mutate
Step 1: retrieve_tools(query="your intent")         → discover tool names  # pragma: no mutate
Step 2: retrieve_tools(exact_tool_names=["TOOL_A"]) → bind for execution  # pragma: no mutate
Step 3: Call the tool directly  # pragma: no mutate

Shortcut: If you already know the exact tool name, skip Step 1 and go straight to binding.  # pragma: no mutate

IF DISCOVERY RETURNS A SUBAGENT (a name starting with "subagent:"): SKIP Step 2  # pragma: no mutate
entirely. Subagents are NOT bound. Go straight to handoff(subagent_id="gmail",  # pragma: no mutate
task="..."). Do NOT call retrieve_tools again to "bind" it, and never call  # pragma: no mutate
retrieve_tools with an empty exact_tool_names. Trying to bind a subagent is the  # pragma: no mutate
single most common mistake here: there is no bind step for a subagent.  # pragma: no mutate

—EFFICIENCY RULES (follow these strictly)  # pragma: no mutate
- Do not call retrieve_tools more than twice for a single task unless the first discovery returned completely irrelevant results  # pragma: no mutate
- Do not discover the same intent twice with different wording unless the first returned nothing useful  # pragma: no mutate
- Do not bind tools you are not going to call immediately  # pragma: no mutate
- Once a tool is bound and returns results, use those results. Do not search for alternative tools.  # pragma: no mutate

—TOOL NAME FORMAT  # pragma: no mutate
Tools follow ALLCAPS_SNAKE_CASE naming: "GITHUB_LIST_PULL_REQUESTS", "GMAIL_SEND_EMAIL"  # pragma: no mutate
Internal tools follow snake_case: "plan_tasks", "vfs_read"  # pragma: no mutate

—ARGS  # pragma: no mutate
query:  # pragma: no mutate
    Natural language description of what you want to do.  # pragma: no mutate
    Be specific about the action and name the integration in plain words  # pragma: no mutate
    ("github", "gmail", "notion") — NOT an id, uuid, or slug. This is semantic  # pragma: no mutate
    vector search; ids do not embed and will not match.  # pragma: no mutate
    Example: "list pull requests", "send email", "create github issue"  # pragma: no mutate

exact_tool_names:  # pragma: no mutate
    List of exact tool names to load and make executable.  # pragma: no mutate
    Example: ["GITHUB_LIST_PULL_REQUESTS", "GITHUB_GET_PULL_REQUEST"]  # pragma: no mutate

—RETURNS  # pragma: no mutate
response: tool names discovered or validated  # pragma: no mutate
tools_to_bind: tools that are now loaded and ready to call  # pragma: no mutate

—EXAMPLES  # pragma: no mutate

Simple read task:  # pragma: no mutate
  retrieve_tools(query="list pull requests")  # pragma: no mutate
  → ["GITHUB_LIST_PULL_REQUESTS", "GITHUB_LIST_PULL_REQUESTS_FOR_REPO", ...]  # pragma: no mutate
  retrieve_tools(exact_tool_names=["GITHUB_LIST_PULL_REQUESTS"])  # pragma: no mutate
  → GITHUB_LIST_PULL_REQUESTS(sort="updated", direction="desc")  # pragma: no mutate
  → First result is the answer. Stop.  # pragma: no mutate

Multi-tool task:  # pragma: no mutate
  retrieve_tools(query="fetch emails, send reply")  # pragma: no mutate
  → ["GMAIL_FETCH_MESSAGES", "GMAIL_REPLY_TO_THREAD", ...]  # pragma: no mutate
  retrieve_tools(exact_tool_names=["GMAIL_FETCH_MESSAGES", "GMAIL_REPLY_TO_THREAD"])  # pragma: no mutate
  → GMAIL_FETCH_MESSAGES(...) → find the thread  # pragma: no mutate
  → GMAIL_REPLY_TO_THREAD(...) → send reply. Done.  # pragma: no mutate

Write task with verification:  # pragma: no mutate
  retrieve_tools(query="list branches, create pull request")  # pragma: no mutate
  → ["GITHUB_LIST_BRANCHES", "GITHUB_CREATE_PULL_REQUEST", ...]  # pragma: no mutate
  retrieve_tools(exact_tool_names=["GITHUB_LIST_BRANCHES", "GITHUB_CREATE_PULL_REQUEST"])  # pragma: no mutate
  → GITHUB_LIST_BRANCHES(...) → confirm branch name  # pragma: no mutate
  → GITHUB_CREATE_PULL_REQUEST(...) → done.  # pragma: no mutate
"""  # pragma: no mutate

_RETRIEVE_TOOLS_SUBAGENT_SECTION = """  # pragma: no mutate

—SUBAGENT TOOLS  # pragma: no mutate
Discovery may also return subagent tools alongside regular tools.  # pragma: no mutate
- Subagent tool format: "subagent:gmail", "subagent:fb9dfd7e05f8"  # pragma: no mutate
- To USE a subagent, call handoff(subagent_id="gmail", task="...") directly.  # pragma: no mutate
- Do NOT pass subagent names back to retrieve_tools, and do NOT try to "bind" them  # pragma: no mutate
  with exact_tool_names. Subagents are never bound, only handed off to; there is no  # pragma: no mutate
  binding step, handoff works immediately on the subagent id (the part after  # pragma: no mutate
  "subagent:").  # pragma: no mutate
- They cannot be executed directly as tools."""  # pragma: no mutate


class ScoredToolHit(TypedDict):  # pragma: no mutate
    """One ranked discovery hit, threaded from a search result to the final list.  # pragma: no mutate

    ``id`` is either a tool name or a ``subagent:<id> (Name)`` key; ``score`` is  # pragma: no mutate
    the backing store's relevance, absent on stores that don't rank.  # pragma: no mutate
    """  # pragma: no mutate

    id: str  # pragma: no mutate
    score: float | None  # pragma: no mutate


# What one entry of the gathered search fan-out yields: Chroma's typed
# ``SearchItem``s from the tool namespaces, or the public-integration store's
# raw dicts. Kept as a union because the two backends genuinely differ; the
# consumer discriminates on the first element and narrows with ``cast``.
SearchTaskResult: TypeAlias = Union[list[SearchItem], list[dict[str, Any]]]  # pragma: no mutate


async def _resolve_connected_subagents(user_id: str) -> dict[str, str | None]:  # pragma: no mutate
    """Map connected integration id -> display name. Platform/internal names come  # pragma: no mutate
    from the in-memory registry; custom MCP names from the cached user-integration  # pragma: no mutate
    list (one read), avoiding an uncached per-MCP lookup on this hot path."""  # pragma: no mutate
    status = await get_all_integrations_status(user_id)  # pragma: no mutate
    connected: dict[str, str | None] = {}  # pragma: no mutate
    custom_connected_ids: list[str] = []  # pragma: no mutate
    for integration_id, is_connected in status.items():  # pragma: no mutate
        if not is_connected:  # pragma: no mutate
            continue  # pragma: no mutate
        platform = get_subagent_by_id(integration_id)  # pragma: no mutate
        if platform:  # pragma: no mutate
            connected[platform.id] = platform.name  # pragma: no mutate
        else:  # pragma: no mutate
            custom_connected_ids.append(integration_id)  # pragma: no mutate

    if custom_connected_ids:  # pragma: no mutate
        user_ints = await get_user_integrations(user_id)  # pragma: no mutate
        names = {r.integration_id: r.integration.name for r in user_ints.integrations}  # pragma: no mutate
        for cid in custom_connected_ids:  # pragma: no mutate
            connected[cid] = names.get(cid)  # pragma: no mutate

    return connected  # pragma: no mutate
mutants_x__get_user_context__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_user_context__mutmut)
async def _get_user_context(  # pragma: no mutate
    user_id: str | None,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
) -> tuple[set[str], dict[str, str | None], set[str]]:  # pragma: no mutate
    """Get user's available namespaces and connected integrations.  # pragma: no mutate

    When include_subagents is False, skips computing subagent-related data  # pragma: no mutate
    entirely — no integration queries, no internal subagent resolution.  # pragma: no mutate

    Returns:  # pragma: no mutate
        Tuple of (user_namespaces, connected_integrations, internal_subagents)  # pragma: no mutate
        where connected_integrations maps canonical integration id -> display name.  # pragma: no mutate
    """  # pragma: no mutate
    # Seed namespaces:
    # - "general" is always available (core tools).
    # - tool_space is seeded ONLY when it belongs to a platform integration
    #   (hardcoded in OAUTH_INTEGRATIONS). For custom MCPs / user-owned
    #   integrations the namespace is user-scoped, so it must come from
    #   user_namespaces and not be implicitly granted by the seed —
    #   otherwise one user could search another user's MCP tools.
    user_namespaces: set[str] = {"general"}  # pragma: no mutate
    if _is_platform_tool_space(tool_space):  # pragma: no mutate
        user_namespaces.add(tool_space)  # pragma: no mutate

    connected_integrations: dict[str, str | None] = {}  # pragma: no mutate
    internal_subagents: set[str] = set()  # pragma: no mutate

    # Only compute subagent data when subagents are included
    if include_subagents:  # pragma: no mutate
        internal_subagents = {sa.id for sa in all_subagents() if sa.managed_by == "internal"}  # pragma: no mutate

    if not user_id:  # pragma: no mutate
        return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

    try:  # pragma: no mutate
        # Union (not assign) so platform seeds survive cache contents.
        # Custom MCP namespaces only enter via the cache lookup, which is
        # the user-scoped source of truth — these still gate tool search.
        user_namespaces |= set(await get_user_available_tool_namespaces(user_id))  # pragma: no mutate

        if include_subagents:  # pragma: no mutate
            connected_integrations = await _resolve_connected_subagents(user_id)  # pragma: no mutate
            log.info(  # pragma: no mutate
                f"{LogTag.TOOL} User connected subagents",  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                connected_integrations=sorted(set(connected_integrations)),  # pragma: no mutate
            )  # pragma: no mutate

        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} User namespaces resolved", user_id=user_id, namespaces=user_namespaces  # pragma: no mutate
        )  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(f"{LogTag.TOOL} Failed to get user namespaces", error_type=type(e).__name__)  # pragma: no mutate

    return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate


async def x__get_user_context__mutmut_orig(  # pragma: no mutate
    user_id: str | None,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
) -> tuple[set[str], dict[str, str | None], set[str]]:  # pragma: no mutate
    """Get user's available namespaces and connected integrations.  # pragma: no mutate

    When include_subagents is False, skips computing subagent-related data  # pragma: no mutate
    entirely — no integration queries, no internal subagent resolution.  # pragma: no mutate

    Returns:  # pragma: no mutate
        Tuple of (user_namespaces, connected_integrations, internal_subagents)  # pragma: no mutate
        where connected_integrations maps canonical integration id -> display name.  # pragma: no mutate
    """  # pragma: no mutate
    # Seed namespaces:
    # - "general" is always available (core tools).
    # - tool_space is seeded ONLY when it belongs to a platform integration
    #   (hardcoded in OAUTH_INTEGRATIONS). For custom MCPs / user-owned
    #   integrations the namespace is user-scoped, so it must come from
    #   user_namespaces and not be implicitly granted by the seed —
    #   otherwise one user could search another user's MCP tools.
    user_namespaces: set[str] = {"general"}  # pragma: no mutate
    if _is_platform_tool_space(tool_space):  # pragma: no mutate
        user_namespaces.add(tool_space)  # pragma: no mutate

    connected_integrations: dict[str, str | None] = {}  # pragma: no mutate
    internal_subagents: set[str] = set()  # pragma: no mutate

    # Only compute subagent data when subagents are included
    if include_subagents:  # pragma: no mutate
        internal_subagents = {sa.id for sa in all_subagents() if sa.managed_by == "internal"}  # pragma: no mutate

    if not user_id:  # pragma: no mutate
        return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

    try:  # pragma: no mutate
        # Union (not assign) so platform seeds survive cache contents.
        # Custom MCP namespaces only enter via the cache lookup, which is
        # the user-scoped source of truth — these still gate tool search.
        user_namespaces |= set(await get_user_available_tool_namespaces(user_id))  # pragma: no mutate

        if include_subagents:  # pragma: no mutate
            connected_integrations = await _resolve_connected_subagents(user_id)  # pragma: no mutate
            log.info(  # pragma: no mutate
                f"{LogTag.TOOL} User connected subagents",  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                connected_integrations=sorted(set(connected_integrations)),  # pragma: no mutate
            )  # pragma: no mutate

        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} User namespaces resolved", user_id=user_id, namespaces=user_namespaces  # pragma: no mutate
        )  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(f"{LogTag.TOOL} Failed to get user namespaces", error_type=type(e).__name__)  # pragma: no mutate

    return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate


async def x__get_user_context__mutmut_1(  # pragma: no mutate
    user_id: str | None,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    include_subagents: bool = False,  # pragma: no mutate
) -> tuple[set[str], dict[str, str | None], set[str]]:  # pragma: no mutate
    """Get user's available namespaces and connected integrations.  # pragma: no mutate

    When include_subagents is False, skips computing subagent-related data  # pragma: no mutate
    entirely — no integration queries, no internal subagent resolution.  # pragma: no mutate

    Returns:  # pragma: no mutate
        Tuple of (user_namespaces, connected_integrations, internal_subagents)  # pragma: no mutate
        where connected_integrations maps canonical integration id -> display name.  # pragma: no mutate
    """  # pragma: no mutate
    # Seed namespaces:
    # - "general" is always available (core tools).
    # - tool_space is seeded ONLY when it belongs to a platform integration
    #   (hardcoded in OAUTH_INTEGRATIONS). For custom MCPs / user-owned
    #   integrations the namespace is user-scoped, so it must come from
    #   user_namespaces and not be implicitly granted by the seed —
    #   otherwise one user could search another user's MCP tools.
    user_namespaces: set[str] = {"general"}  # pragma: no mutate
    if _is_platform_tool_space(tool_space):  # pragma: no mutate
        user_namespaces.add(tool_space)  # pragma: no mutate

    connected_integrations: dict[str, str | None] = {}  # pragma: no mutate
    internal_subagents: set[str] = set()  # pragma: no mutate

    # Only compute subagent data when subagents are included
    if include_subagents:  # pragma: no mutate
        internal_subagents = {sa.id for sa in all_subagents() if sa.managed_by == "internal"}  # pragma: no mutate

    if not user_id:  # pragma: no mutate
        return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

    try:  # pragma: no mutate
        # Union (not assign) so platform seeds survive cache contents.
        # Custom MCP namespaces only enter via the cache lookup, which is
        # the user-scoped source of truth — these still gate tool search.
        user_namespaces |= set(await get_user_available_tool_namespaces(user_id))  # pragma: no mutate

        if include_subagents:  # pragma: no mutate
            connected_integrations = await _resolve_connected_subagents(user_id)  # pragma: no mutate
            log.info(  # pragma: no mutate
                f"{LogTag.TOOL} User connected subagents",  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                connected_integrations=sorted(set(connected_integrations)),  # pragma: no mutate
            )  # pragma: no mutate

        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} User namespaces resolved", user_id=user_id, namespaces=user_namespaces  # pragma: no mutate
        )  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(f"{LogTag.TOOL} Failed to get user namespaces", error_type=type(e).__name__)  # pragma: no mutate

    return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate


async def x__get_user_context__mutmut_2(  # pragma: no mutate
    user_id: str | None,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
) -> tuple[set[str], dict[str, str | None], set[str]]:  # pragma: no mutate
    """Get user's available namespaces and connected integrations.  # pragma: no mutate

    When include_subagents is False, skips computing subagent-related data  # pragma: no mutate
    entirely — no integration queries, no internal subagent resolution.  # pragma: no mutate

    Returns:  # pragma: no mutate
        Tuple of (user_namespaces, connected_integrations, internal_subagents)  # pragma: no mutate
        where connected_integrations maps canonical integration id -> display name.  # pragma: no mutate
    """  # pragma: no mutate
    # Seed namespaces:
    # - "general" is always available (core tools).
    # - tool_space is seeded ONLY when it belongs to a platform integration
    #   (hardcoded in OAUTH_INTEGRATIONS). For custom MCPs / user-owned
    #   integrations the namespace is user-scoped, so it must come from
    #   user_namespaces and not be implicitly granted by the seed —
    #   otherwise one user could search another user's MCP tools.
    user_namespaces: set[str] = {"general"}  # pragma: no mutate
    if _is_platform_tool_space(tool_space):  # pragma: no mutate
        user_namespaces.add(tool_space)  # pragma: no mutate

    connected_integrations: dict[str, str | None] = {}  # pragma: no mutate
    internal_subagents: set[str] = set()  # pragma: no mutate

    # Only compute subagent data when subagents are included
    if include_subagents:  # pragma: no mutate
        internal_subagents = {sa.id for sa in all_subagents() if sa.managed_by == "internal"}  # pragma: no mutate

    if not user_id:  # pragma: no mutate
        return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

    try:  # pragma: no mutate
        # Union (not assign) so platform seeds survive cache contents.
        # Custom MCP namespaces only enter via the cache lookup, which is
        # the user-scoped source of truth — these still gate tool search.
        user_namespaces |= set(await get_user_available_tool_namespaces(user_id))  # pragma: no mutate

        if include_subagents:  # pragma: no mutate
            connected_integrations = await _resolve_connected_subagents(user_id)  # pragma: no mutate
            log.info(  # pragma: no mutate
                f"{LogTag.TOOL} User connected subagents",  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                connected_integrations=sorted(None),  # pragma: no mutate
            )  # pragma: no mutate

        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} User namespaces resolved", user_id=user_id, namespaces=user_namespaces  # pragma: no mutate
        )  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(f"{LogTag.TOOL} Failed to get user namespaces", error_type=type(e).__name__)  # pragma: no mutate

    return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate


async def x__get_user_context__mutmut_3(  # pragma: no mutate
    user_id: str | None,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
) -> tuple[set[str], dict[str, str | None], set[str]]:  # pragma: no mutate
    """Get user's available namespaces and connected integrations.  # pragma: no mutate

    When include_subagents is False, skips computing subagent-related data  # pragma: no mutate
    entirely — no integration queries, no internal subagent resolution.  # pragma: no mutate

    Returns:  # pragma: no mutate
        Tuple of (user_namespaces, connected_integrations, internal_subagents)  # pragma: no mutate
        where connected_integrations maps canonical integration id -> display name.  # pragma: no mutate
    """  # pragma: no mutate
    # Seed namespaces:
    # - "general" is always available (core tools).
    # - tool_space is seeded ONLY when it belongs to a platform integration
    #   (hardcoded in OAUTH_INTEGRATIONS). For custom MCPs / user-owned
    #   integrations the namespace is user-scoped, so it must come from
    #   user_namespaces and not be implicitly granted by the seed —
    #   otherwise one user could search another user's MCP tools.
    user_namespaces: set[str] = {"general"}  # pragma: no mutate
    if _is_platform_tool_space(tool_space):  # pragma: no mutate
        user_namespaces.add(tool_space)  # pragma: no mutate

    connected_integrations: dict[str, str | None] = {}  # pragma: no mutate
    internal_subagents: set[str] = set()  # pragma: no mutate

    # Only compute subagent data when subagents are included
    if include_subagents:  # pragma: no mutate
        internal_subagents = {sa.id for sa in all_subagents() if sa.managed_by == "internal"}  # pragma: no mutate

    if not user_id:  # pragma: no mutate
        return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

    try:  # pragma: no mutate
        # Union (not assign) so platform seeds survive cache contents.
        # Custom MCP namespaces only enter via the cache lookup, which is
        # the user-scoped source of truth — these still gate tool search.
        user_namespaces |= set(await get_user_available_tool_namespaces(user_id))  # pragma: no mutate

        if include_subagents:  # pragma: no mutate
            connected_integrations = await _resolve_connected_subagents(user_id)  # pragma: no mutate
            log.info(  # pragma: no mutate
                f"{LogTag.TOOL} User connected subagents",  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                connected_integrations=sorted(set(None)),  # pragma: no mutate
            )  # pragma: no mutate

        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} User namespaces resolved", user_id=user_id, namespaces=user_namespaces  # pragma: no mutate
        )  # pragma: no mutate
    except Exception as e:  # pragma: no mutate
        log.warning(f"{LogTag.TOOL} Failed to get user namespaces", error_type=type(e).__name__)  # pragma: no mutate

    return user_namespaces, connected_integrations, internal_subagents  # pragma: no mutate

mutants_x__get_user_context__mutmut['_mutmut_orig'] = x__get_user_context__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_user_context__mutmut['x__get_user_context__mutmut_1'] = x__get_user_context__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_user_context__mutmut['x__get_user_context__mutmut_2'] = x__get_user_context__mutmut_2 # type: ignore # mutmut generated
mutants_x__get_user_context__mutmut['x__get_user_context__mutmut_3'] = x__get_user_context__mutmut_3 # type: ignore # mutmut generated
mutants_x__build_search_tasks__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__build_search_tasks__mutmut)
def _build_search_tasks(  # pragma: no mutate
    store: BaseStore,  # pragma: no mutate
    query: str,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    user_namespaces: set[str],  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    limit: int,  # pragma: no mutate
    include_desktop: bool = False,  # pragma: no mutate
) -> list[Awaitable[SearchTaskResult]]:  # pragma: no mutate
    """Build list of search tasks to execute.  # pragma: no mutate

    The `tool_space in user_namespaces` gate is the security boundary that  # pragma: no mutate
    keeps a user from searching another user's custom MCP tools. We never  # pragma: no mutate
    bypass it here — _get_user_context is responsible for ensuring  # pragma: no mutate
    user_namespaces contains tool_space whenever the caller is entitled  # pragma: no mutate
    to search it (always for platform integrations, only when the user  # pragma: no mutate
    has the integration connected for custom MCPs).  # pragma: no mutate
    """  # pragma: no mutate
    search_tasks: list[Awaitable[SearchTaskResult]] = []  # pragma: no mutate

    # Search in tool_space
    if tool_space in user_namespaces or tool_space == "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for tool space", tool_space=tool_space)  # pragma: no mutate
        search_tasks.append(store.asearch((tool_space,), query=query, limit=limit))  # pragma: no mutate
    else:  # pragma: no mutate
        # Caller is in a subagent whose namespace they don't own. This is
        # unusual — usually it means a stale cache or a misrouted handoff.
        # We refuse the search rather than leak another user's tool index.
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools refused search: tool_space not in user_namespaces",  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            user_namespaces=sorted(user_namespaces),  # pragma: no mutate
        )  # pragma: no mutate

    # For subagents, also search 'general' namespace with a small limit
    # so core tools (e.g. webpage tools) are still discoverable.
    if tool_space != "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for general namespace (limited to 5 for core tools)")  # pragma: no mutate
        search_tasks.append(store.asearch(("general",), query=query, limit=5))  # pragma: no mutate

    # Desktop-executed tools are only discoverable for desktop-app sessions
    # (include_desktop is derived from conversation_source upstream).
    if include_desktop:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for desktop namespace")  # pragma: no mutate
        search_tasks.append(store.asearch((DESKTOP_TOOL_SPACE,), query=query, limit=10))  # pragma: no mutate

    # Search subagents namespace
    if include_subagents:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for subagents namespace")  # pragma: no mutate
        search_tasks.append(store.asearch(("subagents",), query=query, limit=15))  # pragma: no mutate
        search_tasks.append(search_public_integrations(query=query, limit=15))  # pragma: no mutate

    return search_tasks  # pragma: no mutate


def x__build_search_tasks__mutmut_orig(  # pragma: no mutate
    store: BaseStore,  # pragma: no mutate
    query: str,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    user_namespaces: set[str],  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    limit: int,  # pragma: no mutate
    include_desktop: bool = False,  # pragma: no mutate
) -> list[Awaitable[SearchTaskResult]]:  # pragma: no mutate
    """Build list of search tasks to execute.  # pragma: no mutate

    The `tool_space in user_namespaces` gate is the security boundary that  # pragma: no mutate
    keeps a user from searching another user's custom MCP tools. We never  # pragma: no mutate
    bypass it here — _get_user_context is responsible for ensuring  # pragma: no mutate
    user_namespaces contains tool_space whenever the caller is entitled  # pragma: no mutate
    to search it (always for platform integrations, only when the user  # pragma: no mutate
    has the integration connected for custom MCPs).  # pragma: no mutate
    """  # pragma: no mutate
    search_tasks: list[Awaitable[SearchTaskResult]] = []  # pragma: no mutate

    # Search in tool_space
    if tool_space in user_namespaces or tool_space == "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for tool space", tool_space=tool_space)  # pragma: no mutate
        search_tasks.append(store.asearch((tool_space,), query=query, limit=limit))  # pragma: no mutate
    else:  # pragma: no mutate
        # Caller is in a subagent whose namespace they don't own. This is
        # unusual — usually it means a stale cache or a misrouted handoff.
        # We refuse the search rather than leak another user's tool index.
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools refused search: tool_space not in user_namespaces",  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            user_namespaces=sorted(user_namespaces),  # pragma: no mutate
        )  # pragma: no mutate

    # For subagents, also search 'general' namespace with a small limit
    # so core tools (e.g. webpage tools) are still discoverable.
    if tool_space != "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for general namespace (limited to 5 for core tools)")  # pragma: no mutate
        search_tasks.append(store.asearch(("general",), query=query, limit=5))  # pragma: no mutate

    # Desktop-executed tools are only discoverable for desktop-app sessions
    # (include_desktop is derived from conversation_source upstream).
    if include_desktop:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for desktop namespace")  # pragma: no mutate
        search_tasks.append(store.asearch((DESKTOP_TOOL_SPACE,), query=query, limit=10))  # pragma: no mutate

    # Search subagents namespace
    if include_subagents:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for subagents namespace")  # pragma: no mutate
        search_tasks.append(store.asearch(("subagents",), query=query, limit=15))  # pragma: no mutate
        search_tasks.append(search_public_integrations(query=query, limit=15))  # pragma: no mutate

    return search_tasks  # pragma: no mutate


def x__build_search_tasks__mutmut_1(  # pragma: no mutate
    store: BaseStore,  # pragma: no mutate
    query: str,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    user_namespaces: set[str],  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    limit: int,  # pragma: no mutate
    include_desktop: bool = True,  # pragma: no mutate
) -> list[Awaitable[SearchTaskResult]]:  # pragma: no mutate
    """Build list of search tasks to execute.  # pragma: no mutate

    The `tool_space in user_namespaces` gate is the security boundary that  # pragma: no mutate
    keeps a user from searching another user's custom MCP tools. We never  # pragma: no mutate
    bypass it here — _get_user_context is responsible for ensuring  # pragma: no mutate
    user_namespaces contains tool_space whenever the caller is entitled  # pragma: no mutate
    to search it (always for platform integrations, only when the user  # pragma: no mutate
    has the integration connected for custom MCPs).  # pragma: no mutate
    """  # pragma: no mutate
    search_tasks: list[Awaitable[SearchTaskResult]] = []  # pragma: no mutate

    # Search in tool_space
    if tool_space in user_namespaces or tool_space == "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for tool space", tool_space=tool_space)  # pragma: no mutate
        search_tasks.append(store.asearch((tool_space,), query=query, limit=limit))  # pragma: no mutate
    else:  # pragma: no mutate
        # Caller is in a subagent whose namespace they don't own. This is
        # unusual — usually it means a stale cache or a misrouted handoff.
        # We refuse the search rather than leak another user's tool index.
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools refused search: tool_space not in user_namespaces",  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            user_namespaces=sorted(user_namespaces),  # pragma: no mutate
        )  # pragma: no mutate

    # For subagents, also search 'general' namespace with a small limit
    # so core tools (e.g. webpage tools) are still discoverable.
    if tool_space != "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for general namespace (limited to 5 for core tools)")  # pragma: no mutate
        search_tasks.append(store.asearch(("general",), query=query, limit=5))  # pragma: no mutate

    # Desktop-executed tools are only discoverable for desktop-app sessions
    # (include_desktop is derived from conversation_source upstream).
    if include_desktop:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for desktop namespace")  # pragma: no mutate
        search_tasks.append(store.asearch((DESKTOP_TOOL_SPACE,), query=query, limit=10))  # pragma: no mutate

    # Search subagents namespace
    if include_subagents:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for subagents namespace")  # pragma: no mutate
        search_tasks.append(store.asearch(("subagents",), query=query, limit=15))  # pragma: no mutate
        search_tasks.append(search_public_integrations(query=query, limit=15))  # pragma: no mutate

    return search_tasks  # pragma: no mutate


def x__build_search_tasks__mutmut_2(  # pragma: no mutate
    store: BaseStore,  # pragma: no mutate
    query: str,  # pragma: no mutate
    tool_space: str,  # pragma: no mutate
    user_namespaces: set[str],  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    limit: int,  # pragma: no mutate
    include_desktop: bool = False,  # pragma: no mutate
) -> list[Awaitable[SearchTaskResult]]:  # pragma: no mutate
    """Build list of search tasks to execute.  # pragma: no mutate

    The `tool_space in user_namespaces` gate is the security boundary that  # pragma: no mutate
    keeps a user from searching another user's custom MCP tools. We never  # pragma: no mutate
    bypass it here — _get_user_context is responsible for ensuring  # pragma: no mutate
    user_namespaces contains tool_space whenever the caller is entitled  # pragma: no mutate
    to search it (always for platform integrations, only when the user  # pragma: no mutate
    has the integration connected for custom MCPs).  # pragma: no mutate
    """  # pragma: no mutate
    search_tasks: list[Awaitable[SearchTaskResult]] = []  # pragma: no mutate

    # Search in tool_space
    if tool_space in user_namespaces or tool_space == "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for tool space", tool_space=tool_space)  # pragma: no mutate
        search_tasks.append(store.asearch((tool_space,), query=query, limit=limit))  # pragma: no mutate
    else:  # pragma: no mutate
        # Caller is in a subagent whose namespace they don't own. This is
        # unusual — usually it means a stale cache or a misrouted handoff.
        # We refuse the search rather than leak another user's tool index.
        log.warning(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools refused search: tool_space not in user_namespaces",  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            user_namespaces=sorted(None),  # pragma: no mutate
        )  # pragma: no mutate

    # For subagents, also search 'general' namespace with a small limit
    # so core tools (e.g. webpage tools) are still discoverable.
    if tool_space != "general":  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for general namespace (limited to 5 for core tools)")  # pragma: no mutate
        search_tasks.append(store.asearch(("general",), query=query, limit=5))  # pragma: no mutate

    # Desktop-executed tools are only discoverable for desktop-app sessions
    # (include_desktop is derived from conversation_source upstream).
    if include_desktop:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for desktop namespace")  # pragma: no mutate
        search_tasks.append(store.asearch((DESKTOP_TOOL_SPACE,), query=query, limit=10))  # pragma: no mutate

    # Search subagents namespace
    if include_subagents:  # pragma: no mutate
        log.info(f"{LogTag.TOOL} Adding search for subagents namespace")  # pragma: no mutate
        search_tasks.append(store.asearch(("subagents",), query=query, limit=15))  # pragma: no mutate
        search_tasks.append(search_public_integrations(query=query, limit=15))  # pragma: no mutate

    return search_tasks  # pragma: no mutate

mutants_x__build_search_tasks__mutmut['_mutmut_orig'] = x__build_search_tasks__mutmut_orig # type: ignore # mutmut generated
mutants_x__build_search_tasks__mutmut['x__build_search_tasks__mutmut_1'] = x__build_search_tasks__mutmut_1 # type: ignore # mutmut generated
mutants_x__build_search_tasks__mutmut['x__build_search_tasks__mutmut_2'] = x__build_search_tasks__mutmut_2 # type: ignore # mutmut generated


def _process_public_integration_result(  # pragma: no mutate
    result: list[dict[str, Any]],  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process public integration search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        integration_id = item.get("integration_id")  # pragma: no mutate
        if integration_id:  # pragma: no mutate
            # Include name for LLM readability
            name = item.get("name")  # pragma: no mutate
            subagent_key = (  # pragma: no mutate
                f"subagent:{integration_id} ({name})" if name else f"subagent:{integration_id}"  # pragma: no mutate
            )  # pragma: no mutate
            processed.append(ScoredToolHit(id=subagent_key, score=item.get("relevance_score", 0)))  # pragma: no mutate

    return processed  # pragma: no mutate
mutants_x__process_chroma_search_result__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__process_chroma_search_result__mutmut)
def _process_chroma_search_result(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_orig(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_1(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "XXgeneralXX",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_2(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "GENERAL",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_3(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",) or tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_4(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace") or item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_5(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(None, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_6(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, None)  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_7(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr("namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_8(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, )  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_9(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "XXnamespaceXX")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_10(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "NAMESPACE")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_11(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace != ("general",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_12(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("XXgeneralXX",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_13(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("GENERAL",)  # pragma: no mutate
            and tool_space != "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_14(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space == "general"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_15(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "XXgeneralXX"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate


def x__process_chroma_search_result__mutmut_16(  # pragma: no mutate
    result: list[SearchItem],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process Chroma store search results."""  # pragma: no mutate
    processed: list[ScoredToolHit] = []  # pragma: no mutate

    for item in result:  # pragma: no mutate
        tool_key = str(item.key)  # pragma: no mutate

        # Handle subagent results from subagents namespace
        if hasattr(item, "namespace") and item.namespace == ("subagents",):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate

            # Get display name from item.value if available (stored during indexing)
            name = None  # pragma: no mutate
            if hasattr(item, "value") and isinstance(item.value, dict):  # pragma: no mutate
                name = item.value.get("name")  # pragma: no mutate

            # Build subagent key with display name for LLM readability
            if tool_key.startswith("subagent:"):  # pragma: no mutate
                subagent_key = f"{tool_key} ({name})" if name else tool_key  # pragma: no mutate
            else:  # pragma: no mutate
                subagent_key = f"subagent:{tool_key} ({name})" if name else f"subagent:{tool_key}"  # pragma: no mutate

            processed.append(ScoredToolHit(id=subagent_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Handle keys with subagent: prefix — skip if subagents not included
        if tool_key.startswith("subagent:"):  # pragma: no mutate
            if not include_subagents:  # pragma: no mutate
                continue  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate
            continue  # pragma: no mutate

        # Filter general namespace results for subagents - only allow webpage tools
        if (  # pragma: no mutate
            hasattr(item, "namespace")  # pragma: no mutate
            and item.namespace == ("general",)  # pragma: no mutate
            and tool_space != "GENERAL"  # pragma: no mutate
        ):  # pragma: no mutate
            # Only include webpage tools from general namespace for subagents
            if tool_key not in WEBPAGE_TOOLS:  # pragma: no mutate
                continue  # pragma: no mutate

        # Filter delegated tools in main agent context
        if include_subagents:  # pragma: no mutate
            tool_category_name = tool_registry.get_category_of_tool(tool_key)  # pragma: no mutate
            if tool_category_name:  # pragma: no mutate
                category = tool_registry.get_category(name=tool_category_name)  # pragma: no mutate
                if category and category.is_delegated:  # pragma: no mutate
                    continue  # pragma: no mutate

        # Add regular tools
        if tool_key in available_tool_names:  # pragma: no mutate
            processed.append(ScoredToolHit(id=tool_key, score=item.score))  # pragma: no mutate

    return processed  # pragma: no mutate

mutants_x__process_chroma_search_result__mutmut['_mutmut_orig'] = x__process_chroma_search_result__mutmut_orig # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_1'] = x__process_chroma_search_result__mutmut_1 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_2'] = x__process_chroma_search_result__mutmut_2 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_3'] = x__process_chroma_search_result__mutmut_3 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_4'] = x__process_chroma_search_result__mutmut_4 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_5'] = x__process_chroma_search_result__mutmut_5 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_6'] = x__process_chroma_search_result__mutmut_6 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_7'] = x__process_chroma_search_result__mutmut_7 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_8'] = x__process_chroma_search_result__mutmut_8 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_9'] = x__process_chroma_search_result__mutmut_9 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_10'] = x__process_chroma_search_result__mutmut_10 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_11'] = x__process_chroma_search_result__mutmut_11 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_12'] = x__process_chroma_search_result__mutmut_12 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_13'] = x__process_chroma_search_result__mutmut_13 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_14'] = x__process_chroma_search_result__mutmut_14 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_15'] = x__process_chroma_search_result__mutmut_15 # type: ignore # mutmut generated
mutants_x__process_chroma_search_result__mutmut['x__process_chroma_search_result__mutmut_16'] = x__process_chroma_search_result__mutmut_16 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__process_search_results__mutmut)
async def _process_search_results(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_orig(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_1(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "XXgeneralXX",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_2(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "GENERAL",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_3(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "XXkeyXX": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_4(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "KEY": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_5(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(None),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_6(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "XXnamespaceXX": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_7(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "NAMESPACE": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_8(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(None, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_9(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, None) else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_10(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr("namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_11(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, ) else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_12(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "XXnamespaceXX") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_13(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "NAMESPACE") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_14(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "XXscoreXX": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_15(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "SCORE": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_16(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:21]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(e).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate


async def x__process_search_results__mutmut_17(  # pragma: no mutate
    results: list[SearchTaskResult | BaseException],  # pragma: no mutate
    available_tool_names: set[str],  # pragma: no mutate
    tool_registry: ToolRegistry,  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
) -> list[ScoredToolHit]:  # pragma: no mutate
    """Process all search results and return unified list."""  # pragma: no mutate
    all_results: list[ScoredToolHit] = []  # pragma: no mutate

    for idx, result in enumerate(results):  # pragma: no mutate
        if isinstance(result, BaseException):  # pragma: no mutate
            # Already logged (with type) at the gather site in retrieve_tools.
            continue  # pragma: no mutate

        if not result:  # pragma: no mutate
            continue  # pragma: no mutate

        # Determine result type and process accordingly
        is_public_search = isinstance(result[0], dict)  # pragma: no mutate

        if is_public_search:  # pragma: no mutate
            processed = _process_public_integration_result(cast(list[dict[str, Any]], result))  # pragma: no mutate
        else:  # pragma: no mutate
            items = cast(list[SearchItem], result)  # pragma: no mutate
            try:  # pragma: no mutate
                preview = [  # pragma: no mutate
                    {  # pragma: no mutate
                        "key": str(item.key),  # pragma: no mutate
                        "namespace": item.namespace if hasattr(item, "namespace") else None,  # pragma: no mutate
                        "score": item.score,  # pragma: no mutate
                    }  # pragma: no mutate
                    for item in items[:20]  # pragma: no mutate
                ]  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    hit_count=len(result),  # pragma: no mutate
                    preview=preview,  # pragma: no mutate
                )  # pragma: no mutate
            except Exception as e:  # pragma: no mutate
                log.debug(  # pragma: no mutate
                    f"{LogTag.TOOL} Chroma search raw hits log failed",  # pragma: no mutate
                    task_index=idx,  # pragma: no mutate
                    error_type=type(None).__name__,  # pragma: no mutate
                )  # pragma: no mutate
            processed = _process_chroma_search_result(  # pragma: no mutate
                items,  # pragma: no mutate
                available_tool_names,  # pragma: no mutate
                tool_registry,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
                tool_space,  # pragma: no mutate
            )  # pragma: no mutate

        all_results.extend(processed)  # pragma: no mutate

    return all_results  # pragma: no mutate

mutants_x__process_search_results__mutmut['_mutmut_orig'] = x__process_search_results__mutmut_orig # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_1'] = x__process_search_results__mutmut_1 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_2'] = x__process_search_results__mutmut_2 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_3'] = x__process_search_results__mutmut_3 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_4'] = x__process_search_results__mutmut_4 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_5'] = x__process_search_results__mutmut_5 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_6'] = x__process_search_results__mutmut_6 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_7'] = x__process_search_results__mutmut_7 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_8'] = x__process_search_results__mutmut_8 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_9'] = x__process_search_results__mutmut_9 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_10'] = x__process_search_results__mutmut_10 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_11'] = x__process_search_results__mutmut_11 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_12'] = x__process_search_results__mutmut_12 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_13'] = x__process_search_results__mutmut_13 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_14'] = x__process_search_results__mutmut_14 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_15'] = x__process_search_results__mutmut_15 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_16'] = x__process_search_results__mutmut_16 # type: ignore # mutmut generated
mutants_x__process_search_results__mutmut['x__process_search_results__mutmut_17'] = x__process_search_results__mutmut_17 # type: ignore # mutmut generated


def _deduplicate_and_sort(  # pragma: no mutate
    results: list[ScoredToolHit],  # pragma: no mutate
    limit: int,  # pragma: no mutate
) -> list[str]:  # pragma: no mutate
    """Remove duplicates, sort by score, and return top results."""  # pragma: no mutate
    seen: set[str] = set()  # pragma: no mutate
    unique_results: list[ScoredToolHit] = []  # pragma: no mutate

    for r in results:  # pragma: no mutate
        if r["id"] not in seen:  # pragma: no mutate
            seen.add(r["id"])  # pragma: no mutate
            unique_results.append(r)  # pragma: no mutate
    unique_results.sort(key=lambda x: x["score"] or 0.0, reverse=True)  # pragma: no mutate
    return [str(r["id"]) for r in unique_results[:limit]]  # pragma: no mutate
mutants_x__split_subagent_entry__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__split_subagent_entry__mutmut)
def _split_subagent_entry(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_orig(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_1(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = None
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_2(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail or tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_3(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if "XX (XX" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_4(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" not in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_5(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(None):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_6(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith("XX)XX"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_7(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = None
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_8(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(None, 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_9(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", None)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_10(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_11(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", )
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_12(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.rsplit(" (", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_13(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split("XX (XX", 1)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_14(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 2)
        return subagent_id, name[:-1]
    return tail, None


def x__split_subagent_entry__mutmut_15(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:+1]
    return tail, None


def x__split_subagent_entry__mutmut_16(entry: str) -> tuple[str, str | None]:
    """``subagent:<id> (Name)`` -> (id, name)."""
    tail = entry[len("subagent:") :]
    if " (" in tail and tail.endswith(")"):
        subagent_id, name = tail.split(" (", 1)
        return subagent_id, name[:-2]
    return tail, None

mutants_x__split_subagent_entry__mutmut['_mutmut_orig'] = x__split_subagent_entry__mutmut_orig # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_1'] = x__split_subagent_entry__mutmut_1 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_2'] = x__split_subagent_entry__mutmut_2 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_3'] = x__split_subagent_entry__mutmut_3 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_4'] = x__split_subagent_entry__mutmut_4 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_5'] = x__split_subagent_entry__mutmut_5 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_6'] = x__split_subagent_entry__mutmut_6 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_7'] = x__split_subagent_entry__mutmut_7 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_8'] = x__split_subagent_entry__mutmut_8 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_9'] = x__split_subagent_entry__mutmut_9 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_10'] = x__split_subagent_entry__mutmut_10 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_11'] = x__split_subagent_entry__mutmut_11 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_12'] = x__split_subagent_entry__mutmut_12 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_13'] = x__split_subagent_entry__mutmut_13 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_14'] = x__split_subagent_entry__mutmut_14 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_15'] = x__split_subagent_entry__mutmut_15 # type: ignore # mutmut generated
mutants_x__split_subagent_entry__mutmut['x__split_subagent_entry__mutmut_16'] = x__split_subagent_entry__mutmut_16 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__render_discovery_response__mutmut)
def _render_discovery_response(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_orig(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_1(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = None
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_2(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = None
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_3(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith(None):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_4(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("XXsubagent:XX"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_5(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("SUBAGENT:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_6(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(None)
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_7(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(None))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_8(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(None)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_9(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = None
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_10(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(None)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_11(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = None
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_12(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(None)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_13(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = None
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_14(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "XXnameXX": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_15(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "NAME": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_16(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "XXsourceXX": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_17(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "SOURCE": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_18(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) and category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_19(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(None) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_20(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category not in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_21(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "XXgaiaXX",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_22(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "GAIA",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_23(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta or meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_24(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = None
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_25(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["XXneeds_approvalXX"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_26(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["NEEDS_APPROVAL"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_27(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = False
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_28(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"XXidXX": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_29(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"ID": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_30(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "XXnameXX": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_31(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "NAME": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_32(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"XXidXX": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_33(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"ID": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_34(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = None
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_35(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(None, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_36(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, None) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_37(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_38(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, ) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_39(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s not in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_40(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = None
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_41(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(None, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_42(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, None)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_43(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_44(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, )
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_45(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations or s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_46(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_47(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_48(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = None

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_49(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(None, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_50(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, None)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_51(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_52(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, )
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_53(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations or s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_54(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_55(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_56(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = None
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_57(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "XXtools_to_bindXX": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_58(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "TOOLS_TO_BIND": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_59(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(None) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_60(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "XXsubagents_builtinXX": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_61(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "SUBAGENTS_BUILTIN": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_62(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "XXsubagents_connectedXX": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_63(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "SUBAGENTS_CONNECTED": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_64(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "XXsubagents_needing_connectionXX": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_65(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "SUBAGENTS_NEEDING_CONNECTION": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_66(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates >= limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_67(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = None

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_68(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["XXtruncatedXX"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_69(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["TRUNCATED"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_70(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"XXshownXX": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_71(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"SHOWN": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_72(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "XXtotalXX": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_73(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "TOTAL": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_74(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates != 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_75(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 1:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_76(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = None
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_77(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["XXsearch_matched_nothingXX"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_78(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["SEARCH_MATCHED_NOTHING"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_79(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = False
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_80(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = None
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_81(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["XXnextXX"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_82(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["NEXT"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_83(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "XXThe search matched NOTHING; anything listed above is a built-in that is XX"
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_84(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "the search matched nothing; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_85(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "THE SEARCH MATCHED NOTHING; ANYTHING LISTED ABOVE IS A BUILT-IN THAT IS "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_86(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "XXalways offered, not a hit. Retry ONCE with a broader query naming the XX"
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_87(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. retry once with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_88(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "ALWAYS OFFERED, NOT A HIT. RETRY ONCE WITH A BROADER QUERY NAMING THE "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_89(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "XXaction ('send email', not a product name). If you already know the exact XX"
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_90(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). if you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_91(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "ACTION ('SEND EMAIL', NOT A PRODUCT NAME). IF YOU ALREADY KNOW THE EXACT "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_92(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "XXtool name, skip search and call retrieve_tools(exact_tool_names=[...]). XX"
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_93(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "TOOL NAME, SKIP SEARCH AND CALL RETRIEVE_TOOLS(EXACT_TOOL_NAMES=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_94(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "XXOtherwise tell the user the capability is unavailable. Never repeat the XX"
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_95(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "otherwise tell the user the capability is unavailable. never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_96(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "OTHERWISE TELL THE USER THE CAPABILITY IS UNAVAILABLE. NEVER REPEAT THE "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_97(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "XXsame query.XX"
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_98(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "SAME QUERY."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_99(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = None
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_100(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["XXnextXX"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_101(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["NEXT"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_102(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "XXBind with retrieve_tools(exact_tool_names=[...]) then call the tool. XX"
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_103(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_104(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "BIND WITH RETRIEVE_TOOLS(EXACT_TOOL_NAMES=[...]) THEN CALL THE TOOL. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_105(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'XXSubagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). XX'
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_106(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'subagents are not bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_107(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'SUBAGENTS ARE NOT BINDABLE — USE HANDOFF(SUBAGENT_ID="<ID>", TASK="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_108(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "XXAnything under subagents_needing_connection is unusable until the user XX"
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_109(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_110(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "ANYTHING UNDER SUBAGENTS_NEEDING_CONNECTION IS UNUSABLE UNTIL THE USER "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_111(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "XXconnects it, so ask them first.XX"
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_112(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "CONNECTS IT, SO ASK THEM FIRST."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_113(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = None
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_114(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["XXqueryXX"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_115(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["QUERY"] = query
    return json.dumps(payload, indent=2)


def x__render_discovery_response__mutmut_116(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(None, indent=2)


def x__render_discovery_response__mutmut_117(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=None)


def x__render_discovery_response__mutmut_118(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(indent=2)


def x__render_discovery_response__mutmut_119(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, )


def x__render_discovery_response__mutmut_120(
    final_tools: list[str],
    tool_registry: ToolRegistry,
    connected_integrations: dict[str, str | None],
    internal_subagents: set[str],
    query: str | None,
    total_candidates: int,
    limit: int,
) -> str:
    """Render discovery hits as JSON in three buckets: bind, handoff, connect.

    Availability is the only axis that changes what the model may do next, so it
    is the top-level split. ``internal_subagents`` is required to tell a built-in
    capability (always usable) from an integration the user has not connected —
    without it every built-in was reported as needing a connection it has none of.
    """
    bindable: list[str] = []
    subagents: list[tuple[str, str | None]] = []
    for entry in final_tools:
        if entry.startswith("subagent:"):
            subagents.append(_split_subagent_entry(entry))
        else:
            bindable.append(entry)

    def _tool_entry(name: str) -> dict[str, Any]:
        category = tool_registry.get_category_of_tool(name)
        meta = tool_registry.get_tool_meta(name)
        entry: dict[str, Any] = {
            "name": name,
            "source": connected_integrations.get(category) or category
            if category in connected_integrations
            else "gaia",
        }
        if meta and meta.destructive:
            entry["needs_approval"] = True
        return entry

    def _subagent_entry(sid: str, name: str | None) -> dict[str, str]:
        return {"id": sid, "name": name} if name else {"id": sid}

    ready = [_subagent_entry(s, n) for s, n in subagents if s in internal_subagents]
    connected = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s in connected_integrations and s not in internal_subagents
    ]
    needs_connecting = [
        _subagent_entry(s, n)
        for s, n in subagents
        if s not in connected_integrations and s not in internal_subagents
    ]

    payload: dict[str, Any] = {
        "tools_to_bind": [_tool_entry(n) for n in bindable],
        "subagents_builtin": ready,
        "subagents_connected": connected,
        "subagents_needing_connection": needs_connecting,
    }
    if total_candidates > limit:
        payload["truncated"] = {"shown": len(final_tools), "total": total_candidates}

    # Keyed off the SEARCH, not off what is listed: built-in subagents are injected
    # unconditionally, so a zero-match search still returns entries. Reporting that
    # as a find is what sent the model into re-querying the same dead index.
    if total_candidates == 0:
        payload["search_matched_nothing"] = True
        payload["next"] = (
            "The search matched NOTHING; anything listed above is a built-in that is "
            "always offered, not a hit. Retry ONCE with a broader query naming the "
            "action ('send email', not a product name). If you already know the exact "
            "tool name, skip search and call retrieve_tools(exact_tool_names=[...]). "
            "Otherwise tell the user the capability is unavailable. Never repeat the "
            "same query."
        )
    else:
        payload["next"] = (
            "Bind with retrieve_tools(exact_tool_names=[...]) then call the tool. "
            'Subagents are NOT bindable — use handoff(subagent_id="<id>", task="..."). '
            "Anything under subagents_needing_connection is unusable until the user "
            "connects it, so ask them first."
        )
    if query:
        payload["query"] = query
    return json.dumps(payload, indent=3)

mutants_x__render_discovery_response__mutmut['_mutmut_orig'] = x__render_discovery_response__mutmut_orig # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_1'] = x__render_discovery_response__mutmut_1 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_2'] = x__render_discovery_response__mutmut_2 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_3'] = x__render_discovery_response__mutmut_3 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_4'] = x__render_discovery_response__mutmut_4 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_5'] = x__render_discovery_response__mutmut_5 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_6'] = x__render_discovery_response__mutmut_6 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_7'] = x__render_discovery_response__mutmut_7 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_8'] = x__render_discovery_response__mutmut_8 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_9'] = x__render_discovery_response__mutmut_9 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_10'] = x__render_discovery_response__mutmut_10 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_11'] = x__render_discovery_response__mutmut_11 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_12'] = x__render_discovery_response__mutmut_12 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_13'] = x__render_discovery_response__mutmut_13 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_14'] = x__render_discovery_response__mutmut_14 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_15'] = x__render_discovery_response__mutmut_15 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_16'] = x__render_discovery_response__mutmut_16 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_17'] = x__render_discovery_response__mutmut_17 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_18'] = x__render_discovery_response__mutmut_18 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_19'] = x__render_discovery_response__mutmut_19 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_20'] = x__render_discovery_response__mutmut_20 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_21'] = x__render_discovery_response__mutmut_21 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_22'] = x__render_discovery_response__mutmut_22 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_23'] = x__render_discovery_response__mutmut_23 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_24'] = x__render_discovery_response__mutmut_24 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_25'] = x__render_discovery_response__mutmut_25 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_26'] = x__render_discovery_response__mutmut_26 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_27'] = x__render_discovery_response__mutmut_27 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_28'] = x__render_discovery_response__mutmut_28 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_29'] = x__render_discovery_response__mutmut_29 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_30'] = x__render_discovery_response__mutmut_30 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_31'] = x__render_discovery_response__mutmut_31 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_32'] = x__render_discovery_response__mutmut_32 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_33'] = x__render_discovery_response__mutmut_33 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_34'] = x__render_discovery_response__mutmut_34 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_35'] = x__render_discovery_response__mutmut_35 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_36'] = x__render_discovery_response__mutmut_36 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_37'] = x__render_discovery_response__mutmut_37 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_38'] = x__render_discovery_response__mutmut_38 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_39'] = x__render_discovery_response__mutmut_39 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_40'] = x__render_discovery_response__mutmut_40 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_41'] = x__render_discovery_response__mutmut_41 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_42'] = x__render_discovery_response__mutmut_42 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_43'] = x__render_discovery_response__mutmut_43 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_44'] = x__render_discovery_response__mutmut_44 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_45'] = x__render_discovery_response__mutmut_45 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_46'] = x__render_discovery_response__mutmut_46 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_47'] = x__render_discovery_response__mutmut_47 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_48'] = x__render_discovery_response__mutmut_48 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_49'] = x__render_discovery_response__mutmut_49 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_50'] = x__render_discovery_response__mutmut_50 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_51'] = x__render_discovery_response__mutmut_51 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_52'] = x__render_discovery_response__mutmut_52 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_53'] = x__render_discovery_response__mutmut_53 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_54'] = x__render_discovery_response__mutmut_54 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_55'] = x__render_discovery_response__mutmut_55 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_56'] = x__render_discovery_response__mutmut_56 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_57'] = x__render_discovery_response__mutmut_57 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_58'] = x__render_discovery_response__mutmut_58 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_59'] = x__render_discovery_response__mutmut_59 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_60'] = x__render_discovery_response__mutmut_60 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_61'] = x__render_discovery_response__mutmut_61 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_62'] = x__render_discovery_response__mutmut_62 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_63'] = x__render_discovery_response__mutmut_63 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_64'] = x__render_discovery_response__mutmut_64 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_65'] = x__render_discovery_response__mutmut_65 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_66'] = x__render_discovery_response__mutmut_66 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_67'] = x__render_discovery_response__mutmut_67 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_68'] = x__render_discovery_response__mutmut_68 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_69'] = x__render_discovery_response__mutmut_69 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_70'] = x__render_discovery_response__mutmut_70 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_71'] = x__render_discovery_response__mutmut_71 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_72'] = x__render_discovery_response__mutmut_72 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_73'] = x__render_discovery_response__mutmut_73 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_74'] = x__render_discovery_response__mutmut_74 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_75'] = x__render_discovery_response__mutmut_75 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_76'] = x__render_discovery_response__mutmut_76 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_77'] = x__render_discovery_response__mutmut_77 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_78'] = x__render_discovery_response__mutmut_78 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_79'] = x__render_discovery_response__mutmut_79 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_80'] = x__render_discovery_response__mutmut_80 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_81'] = x__render_discovery_response__mutmut_81 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_82'] = x__render_discovery_response__mutmut_82 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_83'] = x__render_discovery_response__mutmut_83 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_84'] = x__render_discovery_response__mutmut_84 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_85'] = x__render_discovery_response__mutmut_85 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_86'] = x__render_discovery_response__mutmut_86 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_87'] = x__render_discovery_response__mutmut_87 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_88'] = x__render_discovery_response__mutmut_88 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_89'] = x__render_discovery_response__mutmut_89 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_90'] = x__render_discovery_response__mutmut_90 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_91'] = x__render_discovery_response__mutmut_91 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_92'] = x__render_discovery_response__mutmut_92 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_93'] = x__render_discovery_response__mutmut_93 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_94'] = x__render_discovery_response__mutmut_94 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_95'] = x__render_discovery_response__mutmut_95 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_96'] = x__render_discovery_response__mutmut_96 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_97'] = x__render_discovery_response__mutmut_97 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_98'] = x__render_discovery_response__mutmut_98 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_99'] = x__render_discovery_response__mutmut_99 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_100'] = x__render_discovery_response__mutmut_100 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_101'] = x__render_discovery_response__mutmut_101 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_102'] = x__render_discovery_response__mutmut_102 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_103'] = x__render_discovery_response__mutmut_103 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_104'] = x__render_discovery_response__mutmut_104 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_105'] = x__render_discovery_response__mutmut_105 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_106'] = x__render_discovery_response__mutmut_106 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_107'] = x__render_discovery_response__mutmut_107 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_108'] = x__render_discovery_response__mutmut_108 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_109'] = x__render_discovery_response__mutmut_109 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_110'] = x__render_discovery_response__mutmut_110 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_111'] = x__render_discovery_response__mutmut_111 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_112'] = x__render_discovery_response__mutmut_112 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_113'] = x__render_discovery_response__mutmut_113 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_114'] = x__render_discovery_response__mutmut_114 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_115'] = x__render_discovery_response__mutmut_115 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_116'] = x__render_discovery_response__mutmut_116 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_117'] = x__render_discovery_response__mutmut_117 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_118'] = x__render_discovery_response__mutmut_118 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_119'] = x__render_discovery_response__mutmut_119 # type: ignore # mutmut generated
mutants_x__render_discovery_response__mutmut['x__render_discovery_response__mutmut_120'] = x__render_discovery_response__mutmut_120 # type: ignore # mutmut generated


def _inject_available_subagents(  # pragma: no mutate
    discovered_tools: list[str],  # pragma: no mutate
    internal_subagents: set[str],  # pragma: no mutate
    connected_integrations: dict[str, str | None],  # pragma: no mutate
    include_subagents: bool,  # pragma: no mutate
) -> list[str]:  # pragma: no mutate
    """Inject available subagents that user has access to.  # pragma: no mutate

    Every subagent entry is rendered as ``subagent:<id> (Name)`` whenever a name  # pragma: no mutate
    is known, so the model can tell what ``subagent:<uuid>`` actually is. Names  # pragma: no mutate
    come from the connected-integrations map first, then the in-memory registry.  # pragma: no mutate
    Semantic-search hits often arrive unnamed (a ``subagent:`` key from a tool  # pragma: no mutate
    namespace, or a store that didn't return the value); they get upgraded here  # pragma: no mutate
    and deduped by canonical id so the named and unnamed forms collapse to one.  # pragma: no mutate
    """  # pragma: no mutate
    if not include_subagents:  # pragma: no mutate
        return discovered_tools  # pragma: no mutate

    def _resolve_name(integration_id: str) -> str | None:  # pragma: no mutate
        if connected_integrations.get(integration_id):  # pragma: no mutate
            return connected_integrations[integration_id]  # pragma: no mutate
        sa = get_subagent_by_id(integration_id)  # pragma: no mutate
        return sa.name if sa else None  # pragma: no mutate

    result: list[str] = []  # pragma: no mutate
    seen_ids: set[str] = set()  # pragma: no mutate

    # Pass 1: keep discovered tools in order; upgrade unnamed subagent hits to
    # carry a name when we can resolve one, and dedupe subagents by canonical id.
    for entry in discovered_tools:  # pragma: no mutate
        if not entry.startswith("subagent:"):  # pragma: no mutate
            result.append(entry)  # pragma: no mutate
            continue  # pragma: no mutate
        tail = entry[len("subagent:") :]  # pragma: no mutate
        canonical_id = tail.split(" ", 1)[0]  # pragma: no mutate
        if canonical_id in seen_ids:  # pragma: no mutate
            continue  # pragma: no mutate
        seen_ids.add(canonical_id)  # pragma: no mutate
        already_named = "(" in tail  # pragma: no mutate
        if already_named:  # pragma: no mutate
            result.append(entry)  # pragma: no mutate
        else:  # pragma: no mutate
            name = _resolve_name(canonical_id)  # pragma: no mutate
            result.append(f"subagent:{canonical_id} ({name})" if name else entry)  # pragma: no mutate

    def _add_subagent(integration_id: str, name: str | None) -> None:  # pragma: no mutate
        if integration_id in seen_ids:  # pragma: no mutate
            return  # pragma: no mutate
        subagent_key = (  # pragma: no mutate
            f"subagent:{integration_id} ({name})" if name else f"subagent:{integration_id}"  # pragma: no mutate
        )  # pragma: no mutate
        result.append(subagent_key)  # pragma: no mutate
        seen_ids.add(integration_id)  # pragma: no mutate

    # Add internal subagents (always available) — names come from the registry.
    for integration_id in internal_subagents:  # pragma: no mutate
        sa = get_subagent_by_id(integration_id)  # pragma: no mutate
        _add_subagent(integration_id, sa.name if sa else None)  # pragma: no mutate

    # Add connected integration subagents — names resolved in _get_user_context.
    for integration_id, name in connected_integrations.items():  # pragma: no mutate
        _add_subagent(integration_id, name)  # pragma: no mutate

    return result  # pragma: no mutate
mutants_x_get_retrieve_tools_function__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_retrieve_tools_function__mutmut)
def get_retrieve_tools_function(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_orig(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_1(  # pragma: no mutate
    tool_space: str = "XXgeneralXX",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_2(  # pragma: no mutate
    tool_space: str = "GENERAL",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_3(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = False,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_4(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 26,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_5(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id") and config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_6(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get(None)  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_7(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(None).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_8(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("XXuser_idXX")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_9(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("USER_ID")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_10(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get(None),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_11(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get(None, {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_12(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", None).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_13(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get({}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_14(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", ).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_15(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("XXmetadataXX", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_16(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("METADATA", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_17(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("XXuser_idXX"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_18(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("USER_ID"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_19(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "XXretrieve_tools received no usable argument (an empty XX"  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_20(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "RETRIEVE_TOOLS RECEIVED NO USABLE ARGUMENT (AN EMPTY "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_21(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "XXexact_tool_names counts as none). Next step: pass XX"  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_22(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_23(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "EXACT_TOOL_NAMES COUNTS AS NONE). NEXT STEP: PASS "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_24(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "XXquery='what you want to do' to discover, or XX"  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_25(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "QUERY='WHAT YOU WANT TO DO' TO DISCOVER, OR "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_26(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "XXexact_tool_names=['TOOL_NAME'] to bind a known tool. To use a XX"  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_27(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['tool_name'] to bind a known tool. to use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_28(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "EXACT_TOOL_NAMES=['TOOL_NAME'] TO BIND A KNOWN TOOL. TO USE A "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_29(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "XXsubagent (a 'subagent:' result), do NOT call retrieve_tools XX"  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_30(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do not call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_31(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "SUBAGENT (A 'SUBAGENT:' RESULT), DO NOT CALL RETRIEVE_TOOLS "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_32(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "XXagain; call handoff(subagent_id='gmail', task='...') directly.XX"  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_33(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "AGAIN; CALL HANDOFF(SUBAGENT_ID='GMAIL', TASK='...') DIRECTLY."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_34(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get(None)  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_35(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(None).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_36(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("XXconversation_sourceXX")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_37(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("CONVERSATION_SOURCE")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_38(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP or tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_39(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is not ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_40(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space != "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_41(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "XXgeneralXX"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_42(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "GENERAL"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_43(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_44(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = None
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_45(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled or tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_46(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_47(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(None) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_48(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) != DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_49(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical == tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_50(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = None
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_51(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "XXretrieve_tools binding rejected out-of-scope toolsXX",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_52(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "RETRIEVE_TOOLS BINDING REJECTED OUT-OF-SCOPE TOOLS",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_53(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    modeXX="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_54(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requestedXX=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_55(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_boundXX=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_56(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filteredXX=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_57(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode=None,  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_58(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=None,  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_59(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=None,  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_60(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=None,  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_61(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_62(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_63(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_64(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_65(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="XXbindingXX",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_66(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="BINDING",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_67(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) + len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_68(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "XXSubagents are not bound with retrieve_tools. Call XX"  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_69(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "subagents are not bound with retrieve_tools. call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_70(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "SUBAGENTS ARE NOT BOUND WITH RETRIEVE_TOOLS. CALL "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_71(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "XXhandoff(subagent_id='<id>', task='...') directly, using the XX"  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_72(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "HANDOFF(SUBAGENT_ID='<ID>', TASK='...') DIRECTLY, USING THE "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_73(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "XXpart after 'subagent:'.XX"  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_74(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "PART AFTER 'SUBAGENT:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_75(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "XXThese tools are not available inside this subagent and cannot be XX"  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_76(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "these tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_77(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "THESE TOOLS ARE NOT AVAILABLE INSIDE THIS SUBAGENT AND CANNOT BE "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_78(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(None)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_79(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {'XX, XX'.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_80(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "XXmain executor, not this subagent — do not retry binding them; finish XX"  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_81(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "MAIN EXECUTOR, NOT THIS SUBAGENT — DO NOT RETRY BINDING THEM; FINISH "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_82(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "XXyour task here and let the executor handle them.XX"  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_83(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "YOUR TASK HERE AND LET THE EXECUTOR HANDLE THEM."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_84(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = None
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_85(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(None)
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_86(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(None)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_87(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    None
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_88(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: " - ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_89(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "XXResolved to their canonical names — use these from now on: XX"
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_90(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_91(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "RESOLVED TO THEIR CANONICAL NAMES — USE THESE FROM NOW ON: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_92(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(None)
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_93(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + "XX, XX".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_94(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    None
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_95(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(None)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_96(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {'XX, XX'.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_97(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "XXDo not retry these names; run retrieve_tools(query=...) to find XX"
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_98(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_99(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "DO NOT RETRY THESE NAMES; RUN RETRIEVE_TOOLS(QUERY=...) TO FIND "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_100(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "XXwhat actually exists.XX"
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_101(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "WHAT ACTUALLY EXISTS."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_102(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(None)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_103(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_104(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(None),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_105(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="XX\nXX".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_106(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(None, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_107(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, None, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_108(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, None)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_109(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_110(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_111(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, )  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_112(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query and "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_113(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "XXXX",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_114(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(None),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_115(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(None).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_116(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                modeXX="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_117(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                queryXX=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_118(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_spaceXX=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_119(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_idXX=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_120(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searchedXX=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_121(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discoveredXX=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_122(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hitsXX=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_123(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hitsXX=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_124(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hitsXX=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_125(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filterXX=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_126(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_previewXX=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_127(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode=None,  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_128(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=None,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_129(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=None,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_130(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=None,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_131(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=None,  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_132(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=None,  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_133(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=None,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_134(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=None,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_135(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=None,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_136(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=None,  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_137(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=None,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_138(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_139(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_140(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_141(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_142(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_143(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_144(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_145(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_146(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_147(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_148(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_149(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="XXdiscoveryXX",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_150(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="DISCOVERY",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_151(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(None),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_152(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits != 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_153(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 1:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_154(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                None,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_155(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                None,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_156(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                None,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_157(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                None,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_158(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                None,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_159(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                None,
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_160(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                None,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_161(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_162(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_163(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                internal_subagents,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_164(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                query,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_165(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                len(all_results),
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_166(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                limit,
            ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate


def x_get_retrieve_tools_function__mutmut_167(  # pragma: no mutate
    tool_space: str = "general",  # pragma: no mutate
    include_subagents: bool = True,  # pragma: no mutate
    limit: int = 25,  # pragma: no mutate
    bindable_tool_names: set[str] | None = None,  # pragma: no mutate
) -> Callable[..., Awaitable[RetrieveToolsResult]]:  # pragma: no mutate
    """Get a retrieve_tools function configured for specific context.  # pragma: no mutate

    The ``...`` in the return type is deliberate (Type Safety items 11/14): the  # pragma: no mutate
    result is handed to ``create_agent(retrieve_tools_coroutine=...)``, which  # pragma: no mutate
    wraps it in a ``StructuredTool``. LangGraph then calls it by keyword with  # pragma: no mutate
    ``store``/``config`` injected and the rest supplied by the model, so pinning  # pragma: no mutate
    a parameter list here would describe a call shape that never happens.  # pragma: no mutate

    This unified function handles both tool discovery (semantic search) and tool binding.  # pragma: no mutate
    - When `query` is provided: Returns tool names for discovery (not bound)  # pragma: no mutate
    - When `exact_tool_names` is provided: Binds and returns validated tool names  # pragma: no mutate

    Args:  # pragma: no mutate
        tool_space: Namespace to search for tools  # pragma: no mutate
        include_subagents: Whether to include subagent results in search  # pragma: no mutate
        limit: Maximum number of tool results for semantic search  # pragma: no mutate
        bindable_tool_names: The set of tool names this agent's graph can actually  # pragma: no mutate
            bind and execute. When set (scoped agents like provider subagents),  # pragma: no mutate
            binding validates against it instead of the global registry, so a tool  # pragma: no mutate
            the graph can't execute is never reported as a successful bind. None ->  # pragma: no mutate
            validate against the global registry (main executor/comms carry it).  # pragma: no mutate

    Returns:  # pragma: no mutate
        Configured retrieve_tools coroutine that returns RetrieveToolsResult  # pragma: no mutate
    """  # pragma: no mutate

    async def retrieve_tools(  # pragma: no mutate
        store: Annotated[BaseStore, InjectedStore],  # pragma: no mutate
        config: RunnableConfig,  # pragma: no mutate
        query: str | None = None,  # pragma: no mutate
        # Non-nullable array on purpose. A `list[str] | None` annotation emits an
        # `anyOf: [{array}, {null}]` JSON schema, and MiniMax M3 cannot populate an
        # array wrapped in that nullable union: it sends `exact_tool_names: []`
        # however many names it intends to bind. A plain array schema fixes it, and
        # "no exact tools" is an empty list, not null, so nothing is lost.
        exact_tool_names: list[str] = Field(default_factory=list),  # pragma: no mutate
    ) -> RetrieveToolsResult:  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} retrieve_tools called",  # pragma: no mutate
            query=query,  # pragma: no mutate
            exact_tool_names=exact_tool_names,  # pragma: no mutate
            tool_space=tool_space,  # pragma: no mutate
            include_subagents=include_subagents,  # pragma: no mutate
            user_id=agent_configurable(config).get("user_id")  # pragma: no mutate
            or config.get("metadata", {}).get("user_id"),  # pragma: no mutate
        )  # pragma: no mutate
        if not query and not exact_tool_names:  # pragma: no mutate
            # A no-usable-argument call (commonly retrieve_tools(exact_tool_names=[]),
            # an empty list) must NOT crash the run — that aborts the whole executor
            # turn over a recoverable model slip. Return a corrective hint so the
            # caller self-corrects on its next step instead.
            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=[],  # pragma: no mutate
                response=[  # pragma: no mutate
                    (  # pragma: no mutate
                        "retrieve_tools received no usable argument (an empty "  # pragma: no mutate
                        "exact_tool_names counts as none). Next step: pass "  # pragma: no mutate
                        "query='what you want to do' to discover, or "  # pragma: no mutate
                        "exact_tool_names=['TOOL_NAME'] to bind a known tool. To use a "  # pragma: no mutate
                        "subagent (a 'subagent:' result), do NOT call retrieve_tools "  # pragma: no mutate
                        "again; call handoff(subagent_id='gmail', task='...') directly."  # pragma: no mutate
                    )  # pragma: no mutate
                ],  # pragma: no mutate
            )  # pragma: no mutate

        tool_registry = await get_tool_registry()  # pragma: no mutate
        available_tool_names = tool_registry.get_tool_names()  # pragma: no mutate
        log.info(  # pragma: no mutate
            f"{LogTag.TOOL} Registry available tools",  # pragma: no mutate
            available_tool_count=len(available_tool_names),  # pragma: no mutate
        )  # pragma: no mutate

        # Desktop tools only surface for desktop-app conversations, and only
        # in the main agent context (subagents keep their own tool space).
        conversation_source = ConversationSource.coerce(  # pragma: no mutate
            agent_configurable(config).get("conversation_source")  # pragma: no mutate
        )  # pragma: no mutate
        desktop_enabled = (  # pragma: no mutate
            conversation_source is ConversationSource.DESKTOP and tool_space == "general"  # pragma: no mutate
        )  # pragma: no mutate

        # Get user_id from config (try configurable first, then metadata as fallback)
        user_id = agent_configurable(config).get("user_id")  # pragma: no mutate
        if not user_id:  # pragma: no mutate
            # Fallback to metadata
            user_id = config.get("metadata", {}).get("user_id")  # pragma: no mutate
            if user_id and "configurable" in config:  # pragma: no mutate
                # Update configurable with user_id for consistency
                config["configurable"]["user_id"] = user_id  # pragma: no mutate

        if not user_id:  # pragma: no mutate
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools called with NO user_id (not in configurable or metadata)"  # pragma: no mutate
            )  # pragma: no mutate

        # BINDING MODE: Validate and bind exact tool names
        if exact_tool_names:  # pragma: no mutate
            global_tool_names_set = set(available_tool_names)  # pragma: no mutate
            # Validate against the agent's scoped set, not the global registry —
            # else an out-of-scope tool reports a fake bind the graph then rejects,
            # looping the model bind->reject.
            bindable_set = (  # pragma: no mutate
                bindable_tool_names if bindable_tool_names is not None else global_tool_names_set  # pragma: no mutate
            )  # pragma: no mutate

            mcp_tool_names_set = await _user_mcp_tool_names(user_id)  # pragma: no mutate
            known_by_canonical = canonical_tool_name_map(bindable_set | mcp_tool_names_set)  # pragma: no mutate

            validated_tool_names: list[str] = []  # pragma: no mutate
            unknown_tool_names: list[str] = []  # pragma: no mutate
            out_of_scope_tool_names: list[str] = []  # pragma: no mutate
            requested_subagents: list[str] = []  # pragma: no mutate
            # requested name -> canonical name, for the aliases we silently resolved
            renamed_tools: dict[str, str] = {}
            for tool_name in exact_tool_names:  # pragma: no mutate
                if tool_name.startswith("subagent:"):  # pragma: no mutate
                    # Subagents are handed off to, never bound. When subagents are
                    # available here we surface corrective guidance in the response
                    # instead of echoing the name back as if it bound — that made a
                    # model slip look like a successful bind and relied on downstream
                    # filtering. When subagents aren't available, it's just unknown.
                    if include_subagents:  # pragma: no mutate
                        requested_subagents.append(tool_name)  # pragma: no mutate
                    else:  # pragma: no mutate
                        unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif (  # pragma: no mutate
                    not desktop_enabled  # pragma: no mutate
                    and tool_registry.get_category_of_tool(tool_name) == DESKTOP_TOOL_CATEGORY  # pragma: no mutate
                ):  # pragma: no mutate
                    # Desktop tools must not bind outside desktop sessions —
                    # the tools also re-check the source at execution time.
                    unknown_tool_names.append(tool_name)  # pragma: no mutate
                elif tool_name in bindable_set or tool_name in mcp_tool_names_set:  # pragma: no mutate
                    validated_tool_names.append(tool_name)  # pragma: no mutate
                elif canonical := known_by_canonical.get(tool_name.replace("-", "_")):  # pragma: no mutate
                    validated_tool_names.append(canonical)  # pragma: no mutate
                    if canonical != tool_name:
                        renamed_tools[tool_name] = canonical
                elif tool_name in global_tool_names_set:  # pragma: no mutate
                    # Known globally, but not in this agent's scope.
                    out_of_scope_tool_names.append(tool_name)  # pragma: no mutate
                else:  # pragma: no mutate
                    unknown_tool_names.append(tool_name)  # pragma: no mutate

            if unknown_tool_names:  # pragma: no mutate
                # Surfacing this is important: silently dropping requested tools
                # makes registry-population bugs invisible to operators.
                log.warning(  # pragma: no mutate
                    f"{LogTag.TOOL} retrieve_tools binding dropped unknown tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    unknown=unknown_tool_names,  # pragma: no mutate
                    available_count=len(bindable_set),  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                log.warning(  # pragma: no mutate
                    "retrieve_tools binding rejected out-of-scope tools",  # pragma: no mutate
                    tool_space=tool_space,  # pragma: no mutate
                    out_of_scope=out_of_scope_tool_names,  # pragma: no mutate
                )  # pragma: no mutate

            log.set(  # pragma: no mutate
                tool_retrieval=dict(  # pragma: no mutate
                    mode="binding",  # pragma: no mutate
                    tools_requested=len(exact_tool_names),  # pragma: no mutate
                    tools_bound=len(validated_tool_names),  # pragma: no mutate
                    tools_filtered=len(exact_tool_names) - len(validated_tool_names),  # pragma: no mutate
                )  # pragma: no mutate
            )  # pragma: no mutate

            # Bind valid tools regardless; add corrective guidance for subagent /
            # out-of-scope names so the model takes the right path.
            response = list(validated_tool_names)  # pragma: no mutate
            if requested_subagents:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "Subagents are not bound with retrieve_tools. Call "  # pragma: no mutate
                    "handoff(subagent_id='<id>', task='...') directly, using the "  # pragma: no mutate
                    "part after 'subagent:'."  # pragma: no mutate
                )  # pragma: no mutate
            if out_of_scope_tool_names:  # pragma: no mutate
                response.append(  # pragma: no mutate
                    "These tools are not available inside this subagent and cannot be "  # pragma: no mutate
                    f"bound here: {', '.join(out_of_scope_tool_names)}. They belong to the "  # pragma: no mutate
                    "main executor, not this subagent — do not retry binding them; finish "  # pragma: no mutate
                    "your task here and let the executor handle them."  # pragma: no mutate
                )  # pragma: no mutate

            bind_lines: list[str] = []
            if validated_tool_names:
                bind_lines.append(f"Bound {len(validated_tool_names)} tools — call them directly:")
                bind_lines.extend(f"  - {name}" for name in validated_tool_names)
            if renamed_tools:
                bind_lines.append(
                    "Resolved to their canonical names — use these from now on: "
                    + ", ".join(f"{req} -> {canon}" for req, canon in renamed_tools.items())
                )
            if unknown_tool_names:
                bind_lines.append(
                    f"Not found, nothing bound: {', '.join(unknown_tool_names)}. "
                    "Do not retry these names; run retrieve_tools(query=...) to find "
                    "what actually exists."
                )
            bind_lines.extend(line for line in response if line not in validated_tool_names)

            return RetrieveToolsResult(  # pragma: no mutate
                tools_to_bind=validated_tool_names,  # pragma: no mutate
                response=response,  # pragma: no mutate
                response_text="\n".join(bind_lines),
            )  # pragma: no mutate

        # Get user context (skips subagent computation when include_subagents=False)
        (  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            connected_integrations,  # pragma: no mutate
            internal_subagents,  # pragma: no mutate
        ) = await _get_user_context(user_id, tool_space, include_subagents)  # pragma: no mutate

        # Build and execute search tasks
        search_tasks = _build_search_tasks(  # pragma: no mutate
            store,  # pragma: no mutate
            query or "",  # pragma: no mutate
            tool_space,  # pragma: no mutate
            user_namespaces,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            limit,  # pragma: no mutate
            include_desktop=desktop_enabled,  # pragma: no mutate
        )  # pragma: no mutate

        results = await asyncio.gather(*search_tasks, return_exceptions=True)  # pragma: no mutate

        # Surface search failures instead of treating them as empty namespaces.
        # A partial outage degrades to the namespaces that answered; a total
        # outage must raise so the select_tools retry policy (and ultimately
        # the caller) sees a failure, not a silent "no tools found".
        failures = [r for r in results if isinstance(r, BaseException)]  # pragma: no mutate
        for failure in failures:  # pragma: no mutate
            log.error(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools search task failed",  # pragma: no mutate
                error=str(failure),  # pragma: no mutate
                error_type=type(failure).__name__,  # pragma: no mutate
            )  # pragma: no mutate
        if failures and len(failures) == len(results):  # pragma: no mutate
            raise failures[0]  # pragma: no mutate

        # MCP tool names don't live in the global registry anymore (resilience
        # rewrite removed the per-user mcp_{iid}_{user_id} categories). Union
        # the registry names with the user's live MCPClient tool names so the
        # discovery-mode filter doesn't drop every PostHog/Notion/etc. hit.
        available_tool_names_set = set(available_tool_names) | await _user_mcp_tool_names(user_id)  # pragma: no mutate

        chroma_hits = 0  # pragma: no mutate
        public_hits = 0  # pragma: no mutate
        per_namespace_hits: dict[str, int] = {}  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if not isinstance(result, list) or not result:  # pragma: no mutate
                continue  # pragma: no mutate
            if isinstance(result[0], dict):  # pragma: no mutate
                public_hits += len(result)  # pragma: no mutate
                continue  # pragma: no mutate
            chroma_hits += len(result)  # pragma: no mutate
            for item in result:  # pragma: no mutate
                if not hasattr(item, "namespace"):  # pragma: no mutate
                    continue  # pragma: no mutate
                ns = "::".join(item.namespace) if item.namespace else "default"  # pragma: no mutate
                per_namespace_hits[ns] = per_namespace_hits.get(ns, 0) + 1  # pragma: no mutate

        chroma_preview: list[str] = []  # pragma: no mutate
        for result in results:  # pragma: no mutate
            if isinstance(result, list) and result and not isinstance(result[0], dict):  # pragma: no mutate
                for item in result:  # pragma: no mutate
                    if isinstance(item, dict):  # pragma: no mutate
                        namespace = item.get("namespace")  # pragma: no mutate
                        tool_key = item.get("key")  # pragma: no mutate
                    else:  # pragma: no mutate
                        namespace = getattr(item, "namespace", None)  # pragma: no mutate
                        tool_key = getattr(item, "key", None)  # pragma: no mutate
                    if tool_key is None:  # pragma: no mutate
                        continue  # pragma: no mutate
                    chroma_preview.append(f"{namespace}::{tool_key}")  # pragma: no mutate
                    if len(chroma_preview) >= 10:  # pragma: no mutate
                        break  # pragma: no mutate
            if len(chroma_preview) >= 10:  # pragma: no mutate
                break  # pragma: no mutate

        # Process results
        all_results = await _process_search_results(  # pragma: no mutate
            results,  # pragma: no mutate
            available_tool_names_set,  # pragma: no mutate
            tool_registry,  # pragma: no mutate
            include_subagents,  # pragma: no mutate
            tool_space,  # pragma: no mutate
        )  # pragma: no mutate

        # Deduplicate and sort
        discovered_tools = _deduplicate_and_sort(all_results, limit)  # pragma: no mutate

        # Inject available subagents (no-op when include_subagents=False)
        if include_subagents:  # pragma: no mutate
            final_tools = _inject_available_subagents(  # pragma: no mutate
                discovered_tools,  # pragma: no mutate
                internal_subagents,  # pragma: no mutate
                connected_integrations,  # pragma: no mutate
                include_subagents,  # pragma: no mutate
            )  # pragma: no mutate
        else:  # pragma: no mutate
            final_tools = discovered_tools  # pragma: no mutate

        log.set(  # pragma: no mutate
            tool_retrieval=dict(  # pragma: no mutate
                mode="discovery",  # pragma: no mutate
                query=query,  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
                namespaces_searched=sorted(user_namespaces),  # pragma: no mutate
                tools_discovered=len(final_tools),  # pragma: no mutate
                chroma_hits=chroma_hits,  # pragma: no mutate
                public_hits=public_hits,  # pragma: no mutate
                per_namespace_hits=per_namespace_hits,  # pragma: no mutate
                candidates_after_filter=len(all_results),  # pragma: no mutate
                chroma_preview=chroma_preview,  # pragma: no mutate
            )  # pragma: no mutate
        )  # pragma: no mutate
        if chroma_hits == 0:
            log.warning(  # pragma: no mutate
                f"{LogTag.TOOL} retrieve_tools: 0 ChromaDB hits — check that index_tools_to_store actually wrote docs for this namespace",  # pragma: no mutate
                tool_space=tool_space,  # pragma: no mutate
                user_id=user_id,  # pragma: no mutate
            )  # pragma: no mutate

        return RetrieveToolsResult(  # pragma: no mutate
            tools_to_bind=[],  # pragma: no mutate
            response=final_tools,  # pragma: no mutate
            response_text=_render_discovery_response(
                final_tools,
                tool_registry,
                connected_integrations,
                internal_subagents,
                query,
                len(all_results),
                ),
        )  # pragma: no mutate

    # Assign the LLM-facing docstring from pre-built constants
    if include_subagents:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC + _RETRIEVE_TOOLS_SUBAGENT_SECTION  # pragma: no mutate
    else:  # pragma: no mutate
        retrieve_tools.__doc__ = _RETRIEVE_TOOLS_BASE_DOC  # pragma: no mutate

    return retrieve_tools  # pragma: no mutate

mutants_x_get_retrieve_tools_function__mutmut['_mutmut_orig'] = x_get_retrieve_tools_function__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_1'] = x_get_retrieve_tools_function__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_2'] = x_get_retrieve_tools_function__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_3'] = x_get_retrieve_tools_function__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_4'] = x_get_retrieve_tools_function__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_5'] = x_get_retrieve_tools_function__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_6'] = x_get_retrieve_tools_function__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_7'] = x_get_retrieve_tools_function__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_8'] = x_get_retrieve_tools_function__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_9'] = x_get_retrieve_tools_function__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_10'] = x_get_retrieve_tools_function__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_11'] = x_get_retrieve_tools_function__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_12'] = x_get_retrieve_tools_function__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_13'] = x_get_retrieve_tools_function__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_14'] = x_get_retrieve_tools_function__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_15'] = x_get_retrieve_tools_function__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_16'] = x_get_retrieve_tools_function__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_17'] = x_get_retrieve_tools_function__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_18'] = x_get_retrieve_tools_function__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_19'] = x_get_retrieve_tools_function__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_20'] = x_get_retrieve_tools_function__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_21'] = x_get_retrieve_tools_function__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_22'] = x_get_retrieve_tools_function__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_23'] = x_get_retrieve_tools_function__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_24'] = x_get_retrieve_tools_function__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_25'] = x_get_retrieve_tools_function__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_26'] = x_get_retrieve_tools_function__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_27'] = x_get_retrieve_tools_function__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_28'] = x_get_retrieve_tools_function__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_29'] = x_get_retrieve_tools_function__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_30'] = x_get_retrieve_tools_function__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_31'] = x_get_retrieve_tools_function__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_32'] = x_get_retrieve_tools_function__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_33'] = x_get_retrieve_tools_function__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_34'] = x_get_retrieve_tools_function__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_35'] = x_get_retrieve_tools_function__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_36'] = x_get_retrieve_tools_function__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_37'] = x_get_retrieve_tools_function__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_38'] = x_get_retrieve_tools_function__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_39'] = x_get_retrieve_tools_function__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_40'] = x_get_retrieve_tools_function__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_41'] = x_get_retrieve_tools_function__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_42'] = x_get_retrieve_tools_function__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_43'] = x_get_retrieve_tools_function__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_44'] = x_get_retrieve_tools_function__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_45'] = x_get_retrieve_tools_function__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_46'] = x_get_retrieve_tools_function__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_47'] = x_get_retrieve_tools_function__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_48'] = x_get_retrieve_tools_function__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_49'] = x_get_retrieve_tools_function__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_50'] = x_get_retrieve_tools_function__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_51'] = x_get_retrieve_tools_function__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_52'] = x_get_retrieve_tools_function__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_53'] = x_get_retrieve_tools_function__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_54'] = x_get_retrieve_tools_function__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_55'] = x_get_retrieve_tools_function__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_56'] = x_get_retrieve_tools_function__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_57'] = x_get_retrieve_tools_function__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_58'] = x_get_retrieve_tools_function__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_59'] = x_get_retrieve_tools_function__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_60'] = x_get_retrieve_tools_function__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_61'] = x_get_retrieve_tools_function__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_62'] = x_get_retrieve_tools_function__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_63'] = x_get_retrieve_tools_function__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_64'] = x_get_retrieve_tools_function__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_65'] = x_get_retrieve_tools_function__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_66'] = x_get_retrieve_tools_function__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_67'] = x_get_retrieve_tools_function__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_68'] = x_get_retrieve_tools_function__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_69'] = x_get_retrieve_tools_function__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_70'] = x_get_retrieve_tools_function__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_71'] = x_get_retrieve_tools_function__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_72'] = x_get_retrieve_tools_function__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_73'] = x_get_retrieve_tools_function__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_74'] = x_get_retrieve_tools_function__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_75'] = x_get_retrieve_tools_function__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_76'] = x_get_retrieve_tools_function__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_77'] = x_get_retrieve_tools_function__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_78'] = x_get_retrieve_tools_function__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_79'] = x_get_retrieve_tools_function__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_80'] = x_get_retrieve_tools_function__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_81'] = x_get_retrieve_tools_function__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_82'] = x_get_retrieve_tools_function__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_83'] = x_get_retrieve_tools_function__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_84'] = x_get_retrieve_tools_function__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_85'] = x_get_retrieve_tools_function__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_86'] = x_get_retrieve_tools_function__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_87'] = x_get_retrieve_tools_function__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_88'] = x_get_retrieve_tools_function__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_89'] = x_get_retrieve_tools_function__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_90'] = x_get_retrieve_tools_function__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_91'] = x_get_retrieve_tools_function__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_92'] = x_get_retrieve_tools_function__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_93'] = x_get_retrieve_tools_function__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_94'] = x_get_retrieve_tools_function__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_95'] = x_get_retrieve_tools_function__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_96'] = x_get_retrieve_tools_function__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_97'] = x_get_retrieve_tools_function__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_98'] = x_get_retrieve_tools_function__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_99'] = x_get_retrieve_tools_function__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_100'] = x_get_retrieve_tools_function__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_101'] = x_get_retrieve_tools_function__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_102'] = x_get_retrieve_tools_function__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_103'] = x_get_retrieve_tools_function__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_104'] = x_get_retrieve_tools_function__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_105'] = x_get_retrieve_tools_function__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_106'] = x_get_retrieve_tools_function__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_107'] = x_get_retrieve_tools_function__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_108'] = x_get_retrieve_tools_function__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_109'] = x_get_retrieve_tools_function__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_110'] = x_get_retrieve_tools_function__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_111'] = x_get_retrieve_tools_function__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_112'] = x_get_retrieve_tools_function__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_113'] = x_get_retrieve_tools_function__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_114'] = x_get_retrieve_tools_function__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_115'] = x_get_retrieve_tools_function__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_116'] = x_get_retrieve_tools_function__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_117'] = x_get_retrieve_tools_function__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_118'] = x_get_retrieve_tools_function__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_119'] = x_get_retrieve_tools_function__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_120'] = x_get_retrieve_tools_function__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_121'] = x_get_retrieve_tools_function__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_122'] = x_get_retrieve_tools_function__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_123'] = x_get_retrieve_tools_function__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_124'] = x_get_retrieve_tools_function__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_125'] = x_get_retrieve_tools_function__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_126'] = x_get_retrieve_tools_function__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_127'] = x_get_retrieve_tools_function__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_128'] = x_get_retrieve_tools_function__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_129'] = x_get_retrieve_tools_function__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_130'] = x_get_retrieve_tools_function__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_131'] = x_get_retrieve_tools_function__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_132'] = x_get_retrieve_tools_function__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_133'] = x_get_retrieve_tools_function__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_134'] = x_get_retrieve_tools_function__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_135'] = x_get_retrieve_tools_function__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_136'] = x_get_retrieve_tools_function__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_137'] = x_get_retrieve_tools_function__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_138'] = x_get_retrieve_tools_function__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_139'] = x_get_retrieve_tools_function__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_140'] = x_get_retrieve_tools_function__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_141'] = x_get_retrieve_tools_function__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_142'] = x_get_retrieve_tools_function__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_143'] = x_get_retrieve_tools_function__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_144'] = x_get_retrieve_tools_function__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_145'] = x_get_retrieve_tools_function__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_146'] = x_get_retrieve_tools_function__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_147'] = x_get_retrieve_tools_function__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_148'] = x_get_retrieve_tools_function__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_149'] = x_get_retrieve_tools_function__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_150'] = x_get_retrieve_tools_function__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_151'] = x_get_retrieve_tools_function__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_152'] = x_get_retrieve_tools_function__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_153'] = x_get_retrieve_tools_function__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_154'] = x_get_retrieve_tools_function__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_155'] = x_get_retrieve_tools_function__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_156'] = x_get_retrieve_tools_function__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_157'] = x_get_retrieve_tools_function__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_158'] = x_get_retrieve_tools_function__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_159'] = x_get_retrieve_tools_function__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_160'] = x_get_retrieve_tools_function__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_161'] = x_get_retrieve_tools_function__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_162'] = x_get_retrieve_tools_function__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_163'] = x_get_retrieve_tools_function__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_164'] = x_get_retrieve_tools_function__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_165'] = x_get_retrieve_tools_function__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_166'] = x_get_retrieve_tools_function__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_retrieve_tools_function__mutmut['x_get_retrieve_tools_function__mutmut_167'] = x_get_retrieve_tools_function__mutmut_167 # type: ignore # mutmut generated
