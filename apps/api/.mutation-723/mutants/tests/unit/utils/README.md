# Unit Tests — Utils

Tests for standalone utility functions (`app/utils/`). These are pure functions with no side effects — input goes in, output comes out.

Currently covers command parsing (extracting output-redirect tokens from shell-style strings) and markdown frontmatter parsing. New utility modules should have a corresponding test file added here.
