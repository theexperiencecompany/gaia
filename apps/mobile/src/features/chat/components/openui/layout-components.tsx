/**
 * Layout & Data components — re-exported from the OpenUI config layer.
 *
 * The implementations live in:
 *   apps/mobile/src/config/openui/components/layout.tsx
 *
 * Import from here when you need to reference a specific layout view outside
 * of the generic library (e.g. a dev-preview page or a unit test).
 */
export {
  AccordionView,
  ActionCardView,
  AvatarListView,
  accordionDef,
  accordionSchema,
  actionCardDef,
  actionCardSchema,
  avatarListDef,
  avatarListSchema,
  ComparisonTableView,
  comparisonTableDef,
  comparisonTableSchema,
  DataCardView,
  dataCardDef,
  dataCardSchema,
  FileTreeView,
  fileTreeDef,
  fileTreeSchema,
  KbdBlockView,
  kbdBlockDef,
  kbdBlockSchema,
  ProgressListView,
  progressListDef,
  progressListSchema,
  ResultListView,
  resultListDef,
  resultListSchema,
  SelectableListView,
  StatusCardView,
  selectableListDef,
  selectableListSchema,
  statusCardDef,
  statusCardSchema,
  TabsBlockView,
  TagGroupView,
  tabsBlockDef,
  tabsBlockSchema,
  tagGroupDef,
  tagGroupSchema,
} from "@/config/openui/components/layout";
