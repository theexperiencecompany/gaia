/** The feature key for plain chat messages — the primary usage meter that the
 *  hero gauge, trend, daily bars and upgrade copy are all measured against. */
export const PRIMARY_USAGE_FEATURE = "chat_messages";
