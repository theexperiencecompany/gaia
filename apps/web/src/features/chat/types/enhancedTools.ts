/**
 * Enhanced tool types with integration requirements
 */

export interface EnhancedToolInfo {
  name: string;
  category: string;
  displayName: string;
  iconUrl?: string;
  isLocked: boolean;
}
