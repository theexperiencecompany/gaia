/** The activation checklist's public surface. Nothing else in the app should
 * reach past this file into the feature's internals. */
export { FirstStepsCard } from "./components/FirstStepsCard";
export { FirstStepsWidget } from "./components/FirstStepsWidget";
export { useFirstSteps } from "./hooks/useFirstSteps";
