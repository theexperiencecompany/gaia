// Goals feature is temporarily disabled.
// Original goal detail page (ReactFlow roadmap view) preserved in git history.
// Re-enable by reverting this file to the previous commit.

import { notFound } from "next/navigation";

export default function Page() {
  notFound();
}
