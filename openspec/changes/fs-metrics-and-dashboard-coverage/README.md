# fs-metrics-and-dashboard-coverage

Add gauges (in-flight, last-seen, sandbox pool size) + cumulative counters (op total, bash exit code) + revamp FS Ops dashboard so it stays useful between bursts of activity.
