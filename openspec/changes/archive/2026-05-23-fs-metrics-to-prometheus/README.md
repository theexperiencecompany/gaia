# fs-metrics-to-prometheus

Export FS_OPS measurements as Prometheus histograms + counters alongside wide_events; replaces the Redis ring buffer + dev endpoint approach from e2b-perf-metrics-and-improvements
