# pr-assets

Single shared orphan branch holding images referenced from PR descriptions —
one directory per PR (`<pr-number>-<topic>/`). Keeps binaries out of code
diffs and avoids per-PR asset branches. Never merge this branch.
