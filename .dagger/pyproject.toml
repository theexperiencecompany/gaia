[project]
name = "gaia-ci"
version = "0.1.0"
description = "Dagger CI/CD pipeline for the GAIA monorepo"
requires-python = ">=3.12"
dependencies = ["dagger-io"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gaia_ci"]
