---
title: "CLI Commands"
description: "Full reference for GAIA CLI commands including init, up, setup, start, dev, logs, stop, status, and doctor for managing your instance."
icon: "rectangle-terminal"
---

## Overview

The GAIA CLI provides commands for setup, running, logs, health checks, and shutdown.

```bash
gaia init          # Full setup from scratch
gaia up            # One-command setup: configure, pull and start GAIA (self-host)
gaia setup         # Configure existing repo
gaia start         # Start self-host services
gaia dev           # Developer mode (Nx TUI)
gaia dev full      # Developer mode + workers (Nx TUI)
gaia logs          # Stream logs
gaia stop          # Stop services
gaia status        # Check service health
gaia doctor        # Diagnose a GAIA install
gaia --version     # Show CLI version
gaia --help        # Show all commands
```

## All Commands

| Command          | Description                                                                  |
| ---------------- | ---------------------------------------------------------------------------- |
| `gaia init`      | Full setup from scratch, clone repo, configure env, and initialize services |
| `gaia up`        | One-command setup: configure, pull and start GAIA (self-host)                |
| `gaia setup`     | Configure an existing repository                                             |
| `gaia start`     | Start GAIA services in self-host mode                                        |
| `gaia dev`       | Run developer mode in Nx TUI (`mise dev`)                                    |
| `gaia dev full`  | Run developer mode + workers in Nx TUI (`mise dev:full`)                     |
| `gaia logs`      | Stream logs for currently running services                                   |
| `gaia stop`      | Stop running GAIA services                                                   |
| `gaia status`    | Check health and latency of GAIA services                                    |
| `gaia doctor`    | Diagnose a GAIA install: docker, services and setup checks                   |
| `gaia --version` | Display the current CLI version                                              |
| `gaia --help`    | Show help and available commands                                             |

---

## `gaia init`

Complete first-time setup.

```bash
gaia init
```

**Options:**

| Flag              | Description             |
| ----------------- | ----------------------- |
| `--branch <name>` | Clone a specific branch |

```bash
gaia init --branch master
```

The wizard walks through:

1. Prerequisites (Git and Docker; Mise only for developer mode)
2. Port conflict checks and alternatives
3. Repository setup
4. Environment setup (manual or Infisical)
5. Project setup

---

## `gaia up`

One-command setup: configure, pull and start GAIA (self-host).

```bash
gaia up
```

Combines configuration, image pull/build and startup into a single command.

**Options:**

| Flag                     | Description                                                        |
| ------------------------ | ------------------------------------------------------------------ |
| `--yes`                  | Accept all defaults (no prompts)                                   |
| `--llm-key <key>`        | LLM API key for first boot                                         |
| `--llm-provider <name>`  | Provider for `--llm-key`: `openrouter`, `gemini`, or `custom`      |
| `--api-port <port>`      | Host port for the API                                              |
| `--web-port <port>`      | Host port for the web app                                          |
| `--pull`                 | Pull pre-built images only (fail instead of building)              |
| `-b, --build`            | Build images locally before starting                               |
| `--no-start`             | Configure environment only; do not start services                  |
| `--force-dev-tree`       | Allow operating on a developer checkout that is not the install    |

```bash
# Fully non-interactive first boot
gaia up --yes --llm-key <key> --llm-provider openrouter
```

---

## `gaia setup`

Configure an existing GAIA repository.

```bash
cd /path/to/gaia
gaia setup
```

Use this when you need to reconfigure environment variables, change setup mode, or refresh setup.

---

## `gaia start`

Start GAIA services in **self-host mode**.

```bash
gaia start
```

**Options:**

| Flag      | Description                             |
| --------- | --------------------------------------- |
| `--build` | Rebuild Docker images before starting   |
| `--pull`  | Pull latest base images before starting |

```bash
gaia start --build --pull
```

<Note>
  In developer mode, use `gaia dev` or `gaia dev full` instead of `gaia start`.
</Note>

---

## `gaia dev`

Run developer mode in Nx TUI (foreground).

```bash
gaia dev
```

Equivalent underlying command: `mise dev`

---

## `gaia dev full`

Run developer mode + workers in Nx TUI (foreground).

```bash
gaia dev full
```

Equivalent underlying command: `mise dev:full`

---

## `gaia logs`

Stream logs for currently running services.

```bash
gaia logs
```

- Self-host mode: streams Docker service logs.
- Developer mode: streams Docker logs and, when available, app logs.

---

## `gaia stop`

Stop running GAIA services.

```bash
gaia stop
```

**Options:**

| Flag            | Description                                                                  |
| --------------- | ---------------------------------------------------------------------------- |
| `--force-ports` | Aggressively stop listeners on API/Web ports (may affect non-GAIA processes) |

```bash
gaia stop --force-ports
```

By default, `gaia stop` runs in safe mode and only targets GAIA-managed processes.

---

## `gaia status`

Check health and latency of GAIA services.

```bash
gaia status
```

---

## `gaia doctor`

Diagnose a GAIA installation: docker, services and setup checks.

```bash
gaia doctor
```

Runs ordered checks against your installation and prints one `[ok]`/`[FAIL]`
line per check with a remediation hint (`Fix: …`) on failure:

1. **Docker daemon** — reachable via `docker info`
2. **Compose containers** — expected services per setup mode
   (`gaia-selfhost` or `gaia-dev`) are running
3. **API health** — `/health` answers (honors `infra/docker/.env` port overrides)
4. **Setup readiness** — models/plans seeded, LLM provider configured, admin
   account created, billing enabled; each unconfigured item is reported as a
   warning pointing at the web setup wizard
5. **Web** — reachable on its host port
6. **Disk headroom** — ≥ 2 GB free on the Docker data root

Checks that depend on an earlier failure are skipped instead of failing again.

**Exit code:** `1` only when a *blocking* check fails. Setup-readiness
warnings never affect the exit code, so `gaia doctor` is safe to run in
scripts and CI.

Output is plain text in every environment (TTY or not), so reports can be
piped or pasted directly into bug reports:

```bash
$ gaia doctor

gaia doctor
  [ok]  Docker daemon
  [FAIL] Compose containers (gaia-selfhost) — gaia-web missing
         Fix: Run 'gaia start' to bring the stack up, or check 'docker compose -p gaia-selfhost logs'.
  [ok]  API health (localhost:8000) — HTTP 200
  ...

Summary: 1 blocking.
```

---

## Port Conflict Handling

If required ports are in use, the CLI:

1. Detects the conflicting process
2. Suggests alternative ports
3. Writes overrides to `infra/docker/.env`

Overrides persist across restarts and are reused by `gaia start`, `gaia logs`, and `gaia status`.

---

## Upgrading

### Updating GAIA

```bash
cd /path/to/gaia
git pull
gaia setup
```

### Updating the CLI

<CodeGroup>

```bash npm
npm install -g @heygaia/cli
```

```bash pnpm
pnpm add -g @heygaia/cli
```

```bash bun
bun add -g @heygaia/cli
```

</CodeGroup>

---

## Uninstalling

1. Stop all services: `gaia stop`
2. Delete the repository directory
3. Remove CLI metadata: `rm -rf ~/.gaia`
4. Uninstall CLI:

<CodeGroup>

```bash npm
npm uninstall -g @heygaia/cli
```

```bash pnpm
pnpm remove -g @heygaia/cli
```

```bash bun
bun remove -g @heygaia/cli
```

</CodeGroup>

---

## Troubleshooting

<AccordionGroup>
  <Accordion title="command not found: gaia">
    Reinstall globally and open a new terminal session:

    <CodeGroup>

    ```bash npm
    npm install -g @heygaia/cli
    gaia --help
    ```

    ```bash pnpm
    pnpm add -g @heygaia/cli
    gaia --help
    ```

    ```bash bun
    bun add -g @heygaia/cli
    gaia --help
    ```

    </CodeGroup>

  </Accordion>

<Accordion title="Raw mode is not supported">
  The CLI requires an interactive terminal. Don’t run it in a non-TTY context.
</Accordion>

  <Accordion title="Python 3 not found during setup">
    Install Python through Mise:

    ```bash
    mise install python
    ```

  </Accordion>
</AccordionGroup>
