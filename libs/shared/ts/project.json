{
  "name": "shared-typescript",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "root": "libs/shared/ts",
  "sourceRoot": "libs/shared/ts/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm tsc",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Linting shared-typescript'",
        "cwd": "{projectRoot}"
      }
    }
  }
}
