{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Backend",
            "type": "shell",
            "command": "cd apps/backend && source .venv/bin/activate && python -m uvicorn app.main:app --port 8000 --reload",
            "problemMatcher": [],
            "presentation": {
                "panel": "new"
            }
        },
        {
            "label": "Run Web",
            "type": "shell",
            "command": "cd apps/web && pnpm dev",
            "problemMatcher": [],
            "presentation": {
                "panel": "new"
            }
        }
    ]
}