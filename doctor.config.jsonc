{
  "$schema": "https://react.doctor/schema/config.json",
  // Supply-chain floor: nx@22.7.7 scores 46/100 on Socket's install-behavior
  // axis (build orchestrators legitimately run install scripts); its
  // vulnerability/maintenance/quality axes are 92–100. Identity confirmed —
  // it IS this repo's task runner. Floor lowered 50→45 so the gate judges
  // real supply-chain risk, not orchestrator semantics. Lives at the repo
  // root because the workspace-level supply-chain scan reads only the root
  // config when invoked with --project '*'.
  "supplyChain": {
    "enabled": true,
    "minScore": 45
  },
  "ignore": {
    // Agent-skill bundles: SKILL.md files and the example templates they ship,
    // installed here and read by agents — never imported, built or shipped by
    // this repo. The two trees are copies of one set, and the templates that
    // triggered this (motion/templates/*.tsx) are byte-identical across both,
    // so a finding here is a finding in an upstream file and "fixing" it turns
    // the next skill update into a merge conflict. Same call the other
    // scanners already make for these paths: [tool.ruff] extend-exclude and
    // [tool.bandit] exclude_dirs in pyproject.toml, and biome.json, which
    // excludes .claude and .agents outright.
    "files": [".agents/skills/**", ".claude/skills/**"],
    "overrides": [
      // The bridge `add`/setup wizard collects a URL server's headers and then
      // its name through interactive terminal prompts. server-sequential-
      // independent-await wants these two awaits wrapped in Promise.all, but
      // they each read from stdin — running them concurrently would race two
      // readline prompts on the same terminal and corrupt both. Sequential is
      // the only correct order for interactive I/O, not a missed optimization.
      {
        "files": ["**/commands/bridge/wizard.ts"],
        "rules": ["react-doctor/server-sequential-independent-await"]
      },
      // deliverSources MUST await each send before the next: the bubbles of one
      // logical message have to arrive in published order, so fanning them out
      // with Promise.all (what async-await-in-loop suggests) would reorder the
      // user's message. The sequential await is the correctness guarantee, not
      // an oversight — see the method's own comment. The line is only flagged
      // now because this branch added the isChannel argument to the awaited
      // deliver() call; the loop itself predates it and is unchanged.
      {
        "files": ["libs/shared/ts/src/bots/consumer/outbound-consumer.ts"],
        "rules": ["react-doctor/async-await-in-loop"]
      },
      // Same guarantee for the first-contact fallback in link-codes.ts: when
      // the outbound queue refused the greeting, the bot sends its bubbles
      // itself, and they are a conversation the user reads top to bottom —
      // fanning them out would deliver "here is how to start" before "hi".
      {
        "files": ["libs/shared/ts/src/bots/link-codes.ts"],
        "rules": ["react-doctor/async-await-in-loop"]
      },
      // Tunnel.run() is a reconnection supervisor loop: each iteration awaits
      // one full connection lifecycle (connectOnce resolves only when the
      // socket closes or errors) and then a backoff sleep. async-await-in-loop's
      // Promise.all suggestion is inapplicable — parallelizing would open many
      // simultaneous tunnels. Sequential await is the correctness guarantee.
      {
        "files": ["libs/shared/ts/src/bridge-core/tunnel.ts"],
        "rules": ["react-doctor/async-await-in-loop"]
      },
      // The wake-word controller's lifetime is managed imperatively (start()
      // creates it, stop() disposes it), not by an effect, so the three
      // listeners are attached in start() and released by detachListeners().
      // effect-needs-cleanup only recognises subscribe-and-return-cleanup
      // inside one effect and cannot follow that indirection — but the cleanup
      // is structurally guaranteed: the `enabled` effect's cleanup calls
      // stop(), and stop() calls detachListeners() synchronously before
      // awaiting anything, so no listener outlives its controller on unmount,
      // on an `enabled` flip, or on a failed start.
      //
      // Wiring them in an effect instead (as this branch briefly did) is the
      // shape that is actually unsafe here: a ref assignment schedules no
      // render, so calling the hook's exported start() from an event handler
      // would attach no listeners at all, and the version that "worked" did so
      // only because one effect happened to run after another in the same
      // commit.
      {
        "files": ["src/internal/use-wake-word-base.ts"],
        "rules": ["react-doctor/effect-needs-cleanup"]
      }
    ]
  }
}
